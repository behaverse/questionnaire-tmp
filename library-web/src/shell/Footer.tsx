export function Footer() {
  return (
    <footer className="mt-20 border-t border-rule bg-paper-raised">
      <div className="mx-auto flex max-w-6xl flex-col gap-1 px-6 py-8 sm:flex-row sm:items-center sm:justify-between">
        <p className="font-serif text-sm text-ink-soft">
          Behaverse Questionnaire Library
        </p>
        <p className="text-xs uppercase tracking-[0.14em] text-ink-faint">
          Read-only catalogue
        </p>
      </div>
    </footer>
  )
}
