# Local dev only: the SPA (vite dev :5173) calls the Library API on :8000 (CORS-enabled for :5173).
# Production builds set no VITE_API_BASE_URL → the client defaults to same-origin.
VITE_API_BASE_URL=http://localhost:8000
