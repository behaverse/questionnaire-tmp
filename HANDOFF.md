# Handoff

Orients a new agent (or contributor) to the project's current state. Read this once; then go to `design/00_index.md` for the authoritative design and `plan/01_roadmap.md` for sequencing.

> **Want to try it?** [`docs/testing-participant-flow.md`](docs/testing-participant-flow.md) is a step-by-step guide to run the whole stack locally and walk the participant journey — create an account, pick a questionnaire from the catalogue, complete it, and download your own data.

**Last updated:** 2026-06-24 (latest) — **PARTICIPANT-EXPERIENCE ROADMAP COMPLETE (#1–#8 done).** **Latest (merge `86d4e510`): the quick wins #6/#7/#8** — **#7 (VS):** the resume-runtime (`GET /sessions/{id}/runtime`) + locale-switch endpoints now map `PreflightError → 422 preflight_failed` (shared `_preflight_422`, also used by `new_session`) — a deployment locale the questionnaire lacks no longer **500s + strands the participant on `resume_unreachable`**. **#8 (player):** `resume_unreachable` now offers a **"Start fresh"** button (clears the saved IndexedDB session + re-mints) and a permanent submit failure shows the `return_url` **Done** link — the runner is never a dead-end on the failure paths. **#6 (library):** a richer self-contained **EN-only 6-item `qst_wellbeing@v26.0601`** ("Wellbeing check-in") in **`library/fixtures/demo/`** (outside tests/fixtures; seed via `library ingest library/fixtures/demo --release v26.0601`) — replaces the thin 1-item qst_min in demos. Staged 3 commits + sonnet review APPROVE. VS **199** + web-viewer **252** tests; the demo questionnaire ingests + renders (preview, 6 pages) + deploys live. Memory `project_participant_quick_wins`. **The whole participant-experience roadmap (#1 untangle, #2/#4 pick→run→return, #3 return-URL keystone, #5 Try-it preview, #6/#7/#8 quick wins) is DONE.** **Only remaining:** the deferred **#1-SSO** — a seamless cross-origin auth handoff so authenticated deployments don't re-login on the player. **Live demo:** services on Identity :8100, Library :8000, VS :8001, player :5173, portal :5174, Library web :5175 (CORS allows 5173/5174/5175). Prior slice (#5 "Try it" preview, merge `1efaa0e1`):

**Earlier 2026-06-24 — #5 "TRY IT" PREVIEW (merge `1efaa0e1`):** — anyone can **experience** a Library questionnaire **render-only, no data captured**. Three components: **VS** public `GET /v1/preview/runtime?ref=<id@version>&viewer_id=&viewer_version=&locale=` (builds a runtime from a bare `questionnaire_ref` via a synthesized pseudo-deployment — no deployment/session/auth/storage; reuses the runtime cache); **player** (`web-viewer`) a `?preview=<ref>` no-capture boot path (session id `'preview'` treated like `'fixture'` — no mint, no `/responses`, no IndexedDB; honors `return_url`); **library-web** a **"Try it"** link on the DetailPage (`previewPlayerUrl(ref, lang)` → `${VITE_PLAYER_BASE_URL|:5173}/?preview=<id@version>&locale=<default>&viewer_url=<VS>&return_url=<this page>`). Locale = the questionnaire's own default → **sidesteps #7's locale-500 by construction**. Staged 3 commits + sonnet whole-branch review APPROVE. VS **198** + web-viewer **249** + library-web **62** tests, all builds green; the live preview endpoint + full stack verified (library-web on **:5175**). **Try:** open **http://localhost:5175** (Library) → a questionnaire → **Try it** → runs on :5173 → Done → back. Memory `project_library_try_preview`; spec `docs/superpowers/specs/2026-06-24-library-try-preview-design.md`. **NEXT:** the small wins (#6 a richer seeded questionnaire, #7 VS unsupported-locale 500 → clean 4xx, #8 resume/submit dead-ends), or the deferred **#1-SSO** (cross-origin auth handoff). Prior slice (#1 untangle):

**Earlier 2026-06-24 — #1 UNTANGLE INTO TWO APPS (merge `c83641ad`):** — `web-viewer/` split into **three packages**: **`participant-session/`** (shared `SessionProvider` + Identity client, the single source of truth, consumed by a **source path alias** `@behaverse/participant-session` — no build step), **`participant-app/`** (the **portal**: catalogue/account/my-data, dev **:5174**, launches the player at `VITE_PLAYER_BASE_URL` with a `return_url`), and **`web-viewer/`** (the **player** only, dev **:5173**, `main.tsx` always `<App/>`). Owner chose **two fully separate apps/origins** + **re-login on the player now** (anonymous/invite/demo seamless cross-origin; **authenticated** deployments re-prompt login on the player — seamless SSO handoff deferred as **#1-SSO**) + **shared session lib**. The editor's renderer/scoring `build:lib` is untouched. **CORS now needs BOTH origins** (`IDENTITY_CORS_ORIGINS`/`VS_CORS_ORIGINS` = `http://localhost:5173,http://localhost:5174`) since the portal makes the login/catalogue/my-data calls. participant-app **79** + web-viewer **247** tests (= the old 326, none lost), all builds + build:lib green, both dev servers + cross-origin CORS verified live; staged 3 commits + an opus whole-branch review fix wave (the review caught that `@types/react` auto-installed React as a *peer* into participant-session, so the player needed `resolve.dedupe` too — added). **Try:** open **http://localhost:5174** (portal) → Start → runs on :5173 → Done → back. Memory `project_untangle_two_apps`; spec `docs/superpowers/specs/2026-06-24-untangle-two-apps-design.md`. **NEXT:** #5 library-web "Try" links (+ a VS anonymous preview mint), the small wins (#6 richer seeded questionnaire, #7 VS unsupported-locale 500, #8 resume/submit dead-ends), or the deferred #1-SSO (cross-origin auth handoff). Prior context (#2/#3/#4):

**Earlier 2026-06-24 — #3 KEYSTONE + #2/#4 PICK→RUN→RETURN.** **pick→run→return (#4 + #2 partial, merge `fbd4f16f`)** — the catalogue's **Start** now launches the player with a `return_url` back to `${origin}/?done=<id>`; the player's **Done** (the keystone) returns there; the catalogue greets the returning participant with a dismissable **"All done — pick another"** banner naming the finished questionnaire; and the nav now shows a **clearer signed-in state** (avatar initial + always-visible email — answers the owner's "the home looks the same after login"). Same-origin (the two-origins split is #1, later); web-viewer only; **326** tests + clean build; reviewed APPROVE. Try it: from `http://localhost:5173/` click Start on any card → run → Done returns with the banner (no hand-edited URLs). Memory `project_pick_run_return`; spec/plan `docs/superpowers/{specs,plans}/2026-06-24-pick-run-return*`. **NEXT: #1 — untangle into the two fully separate apps/origins** (the return_url handoff already makes the portal↔player boundary clean), then #5 library-web Try links; remaining #2 polish (runner-inside-shell, richer post-completion) folds into #1. Prior context (the roadmap + the keystone):

**Earlier 2026-06-24 — PARTICIPANT-EXPERIENCE ROADMAP STARTED — #3 RETURN-URL KEYSTONE BUILT + MERGED.** After the owner tried the running app they flagged a set of changes (captured in `docs/participant-app-roadmap.md` + memory `project_participant_app_roadmap`): untangle the participant app from the web-viewer into **two fully separate apps/origins** (owner's choice); redesign the journey; a viewer **return-URL on finish**; participant-app **pick→run→return**; **library-web "Try" links**; + small fixes (richer seeded questionnaire, VS unsupported-locale 500, runner resume/submit dead-ends). Unifying idea: the **viewer is a reusable player** that consumers launch with a return URL and that sends the participant back. **Keystone DONE (merge `0a1bc2f3`):** the **return-URL keystone (#3)** — the player accepts a launch-time **`?return_url=`** (embedder-controlled, validated http(s) via `safeReturnUrl` — open-redirect guard) and shows a manual **"Done"** button on the finished / declined / already-completed screens, so the runner is **never a dead-end**; manual only (no auto-redirect), the per-deployment `redirect_url` (PA-4) untouched & complementary. web-viewer only; **321** tests + clean build; reviewed APPROVE. Memory `project_viewer_return_url`; spec/plan `docs/superpowers/{specs,plans}/2026-06-24-viewer-return-url*`. **NEXT in the roadmap sequence:** #2/#4 — journey redesign + participant-app **pick→run→return** (wire the catalogue Start to launch the player with `&return_url=<back to the app>`), then #1 the two-origins untangle, then #5 library-web Try links. FOLLOWUP from #3's review: a *permanent* submission failure still strands the participant on the Retry screen (folded into roadmap #8). Prior:

**Earlier 2026-06-24 — PARTICIPANT APP — PA-4 (consent gate + completion polish) BUILT + MERGED — the core participant journey is now COMPLETE.** Final core PA slice (merge `df089766`). The runner now honours three deployment fields it had ignored: **Viewer Service** — the deployment gains a `consent` **locale-map** and `POST /v1/sessions/new` returns `consent`/`confirmation_message`/`redirect_url`; **Runner** — a **consent gate** (a `ConsentScreen`, markdown, **before Q1** when the deployment has `consent`; `initialized`/`started` are **deferred to Accept**, which also posts a **`bdm:consented`** event; **Decline** → an exit screen + a **`bdm:consent_declined`** event, no responses) + the **finished screen** finally rendering the deployment's `confirmation_message` (markdown) and honouring `redirect_url` (3 s redirect + a manual "click here" link). No-consent deployments are unchanged. Added **`bdm:consent_declined` to `schemas/events/schema.json`** (the VS validates every events batch). **Lightweight per-deployment consent** — the full lifecycle (versioning / re-consent / withdrawal) stays Phase-5 Platform. VS **194** + web-viewer **315** tests, clean build; subagent-driven (7 TDD tasks); opus whole-branch review **APPROVE** — it caught a Critical the per-task tests missed (the new decline verb was absent from the events schema → the VS would 422 + silently drop the decline event, because App.test stubs `/events` with a blanket 202; fixed + 3 VS validation tests). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-participant-pa-4*`; memory `project_participant_pa_4`. **The Participant App core journey (PA-1 session → PA-2 register+shell → PA-3 change-password → email slice → PA-4 consent) is DONE.** NEXT: the **deep Phase-5 Participant Platform** (study protocol builder, OD-09 assignment scheduler, consent *lifecycle*, notifications, dashboards), or other tracks. FOLLOWUPS: resumed sessions use the default finished screen (confirmation/redirect not carried through resume); the two open robustness items (VS unsupported-locale 500; runner `resume_unreachable` dead-end). Prior:

**Earlier 2026-06-23 — PARTICIPANT APP — EMAIL SLICE (verify-email + password-reset + a real mailer) BUILT + MERGED.** Makes PA-3's deferred email flows usable (merge `9ccf7437`). **Identity** gained a config-selected mailer — **`make_mailer(settings)`** returns an **`SmtpMailer`** when `SMTP_HOST` is set, else a **`ConsoleMailer`** that **logs the link** (so local dev is zero-setup — copy the link from the Identity console; `NullMailer` stays for tests) — plus **`WEB_VIEWER_BASE_URL`** + `SMTP_*` config; register/reset emails now carry real links `{base}/verify-email?token=` / `/reset-password?token=`. **No-enumeration preserved**: `request-password-reset` stays 202-always and a mailer failure there is swallowed+logged; register's send is atomic (rolls back on failure). **Web Viewer** added `verifyEmail`/`requestPasswordReset`/`resetPassword` clients, a **`ResetPasswordView`** (route `/reset-password`: no-token request form + `?token` set-new form, validates ≥8) + a **`VerifyEmailView`** (route `/verify-email?token=`, auto-verifies) + a **"Forgot password?"** login link; shared styles in `account/ui.ts`. **Local testing:** with the default ConsoleMailer the verify/reset link is printed to the Identity terminal — see `docs/testing-participant-flow.md` §6. Identity **56** + web-viewer **305** tests, clean build; subagent-driven (7 TDD tasks); opus whole-branch review **APPROVE-WITH-FOLLOWUPS** (no-enumeration + token-no-leak + mailer-safety verified; fixed 7 doc-string drifts). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-participant-email*`; memory `project_participant_email_slice`. **NEXT (PA track): PA-4** (consent + run polish). Deferred: resend-verification, email-change, HTML email templates. Still-open from manual testing: VS unsupported-locale 500; runner `resume_unreachable` dead-end. Prior:

**Earlier 2026-06-23 — PARTICIPANT APP — PA-3 (change password, logged-in) BUILT + MERGED.** Third Participant App slice (merge `a16ff7c5`) and the **first PA slice to extend Identity** (prior slices only consumed it). A new **`POST /v1/auth/change-password`** in `identity-service` (Bearer access token → user from `claims["sub"]` only, never the body; body `{old_password, new_password≥8}`; verifies the old password via Argon2; `204`; **wrong current → `403 wrong_password`** — deliberately NOT 401, so the web-viewer's `authFetch` doesn't mistake it for token-expiry and silently refresh/retry; new<8 → `422`; no/invalid token → `401`; **no session revocation**). Web Viewer: a `changePassword(authFetch, …)` client + a **"Change password"** section in the signed-in `AccountView` profile (validates new≥8 before the call; "Password updated." on success; mapped errors). **Scope decision:** PA-3 = change-password only; **email verification + forgot/reset-password + a real mailer are DEFERRED to a dedicated "email" slice** — they dead-end locally because the Identity mailer is a `NullMailer` stub (records the token, sends nothing; no SMTP). Identity **49** + web-viewer **290** tests, clean build; subagent-driven (4 TDD tasks); opus whole-branch review **APPROVE-WITH-FOLLOWUPS** (self-scoped auth + the 403-not-401 contract + no account-existence leak verified). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-participant-pa-3*`; memory `project_participant_pa_3`. **NEXT (PA track): the "email" slice** (wire a real SMTP mailer → verify-email + forgot/reset UIs) → PA-4 (consent + run polish). Open robustness followups from PA-2 testing: VS should validate/clean-error a deployment's unsupported `available_locales` (denormaliser currently 500s); the runner's `resume_unreachable` should offer a "start fresh" escape. Prior:

**Earlier 2026-06-23 — PARTICIPANT APP — PA-2 (register + nav shell + consolidated SPA) BUILT + MERGED.** Second Participant App slice (merge `80b480b1`): the web-viewer's **three HTML entries collapsed into ONE SPA**. `index.html` → `SessionProvider` → the runner `<App/>` (when `?deployment`/`?invite`/`?fixture`, full-screen, unchanged) or `<ParticipantApp/>` — a custom **path router** (`src/shell/router.tsx`, no dependency) + a persistent **NavShell** (header nav: Questionnaires `/` · My data `/my-data` · Account `/account`, + a session area with email/Log-out or a Log-in link). New **`AccountView`** (`src/account/`): **register** (validates pw≥8, then **auto-logs-in**) | login toggle, and a profile card when signed in; `register()` added to `src/session/client.ts`. Removed `home.html`/`mydata.html` + their mains + `SessionStrip`; HomeApp→`CatalogueView`, MyDataApp→`MyDataView` (anon → a Log-in prompt linking `/account`). **web-viewer 283 tests + clean single-entry build** (`dist/index.html` only); subagent-driven (7 TDD tasks); opus whole-branch review **APPROVE-WITH-FOLLOWUPS** (routing/auth/a11y verified; fixed a WCAG 2.5.3 label defect + 3 doc inaccuracies — the runner does NOT re-prompt a signed-in participant since PA-1's session restores on boot). Decisions: clean path URLs (web-viewer not deployed → no prod SPA-fallback config needed yet); register auto-logs-in; email verification deferred (no mailer). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-participant-pa-2*`; memory `project_participant_pa_2`. **NEXT = PA-3** (account management: change password, email verification + a real mailer, password reset) → PA-4 (consent + run polish). Prior:

**Earlier 2026-06-23 — PARTICIPANT APP — PA-1 (session foundation) BUILT + MERGED.** First slice of the Participant App track (the consolidation that fixes the fragmented participant front-end). Merge `10899abe`: a shared **persistent session** in the web-viewer (`src/session/`: `storage` localStorage refresh token + `client` Identity login/refresh/logout/me + `authFetch` Bearer-with-single-flight-refresh-on-401 + `SessionProvider`/`useSession` + `SessionStrip` logout). **Login now survives page refreshes + return visits, and there's a working logout** — the in-memory-token fragility that made the participant flow feel broken is gone. Rewired the runner, my-data, and catalogue onto `useSession()` (dropped the ad-hoc `auth.ts`); entries wrapped in `<SessionProvider>`. Refresh token in **localStorage** (owner decision; cross-origin, no backend change; httpOnly-cookie hardening deferred). Backend (Identity) was already complete — PA-1 is pure front-end glue. web-viewer **265 tests** + clean build; subagent-driven (7 TDD tasks); opus whole-branch review **APPROVE-WITH-FOLLOWUPS** (persistence keystone + login re-mint race + access-never-persisted verified). The branch also folded in the **pre-PA-1 UI redesign** (home/my-data/login restyle + root-serves-catalogue routing). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-participant-pa-1*`; memory `project_participant_pa_1` + `project_participant_app_plan`. **NEXT = PA-2** (register UI + unified nav shell + consolidate entries) → PA-3 (account mgmt) → PA-4 (consent). Prior:

**Earlier 2026-06-22 — PARTICIPANT FLOW COMPLETE — PP-D (participant catalogue / "pick a questionnaire") BUILT + MERGED.** Final participant-flow slice (merge `7d43f369`): a participant **browses the questionnaires a researcher chose to publish and starts one**, reusing the existing runner. Because completion only happens via a **deployment**, the catalogue lists deployments. Model decisions: (1) **`listed` opt-in** — a deployment appears only if created `listed=true` (existing deployments default false → no study-link leak); (2) **title/description stored on the deployment** (catalogue falls back to `questionnaire_ref`; no live Library call — auto-fill deferred); (3) the catalogue is **public** (browse without login; auth happens at Start); (4) **Start just links to the runner** (`index.html?deployment=<id>` carrying `viewer_url`/`identity_url`) — no session logic in the catalogue. **VS:** deployment gains `listed`/`title`/`description`; new **public `GET /v1/catalogue`** (`api/catalogue.py`) returns `listed` + open (reuses pure `check_deployable`) + browse-startable (`dimensions.auth IN ('none','identity')` → **demo included, invite_link excluded**) deployments, with a **field allowlist** (no created_by/quota/dimensions/session-count leak; NULL-dimensions rows excluded). **Web Viewer:** a new **`home.html` `HomeApp` portal** (`src/home/`, prod+dev entry) — `fetchCatalogue` (no-auth GET) → cards with a **Start** link + a **"My data"** link → `mydata.html`; `carry()` forwards only `viewer_url`/`identity_url` (whitelist, no token leak). VS **189** + web-viewer **245** + clean build (`dist/home.html`); opus whole-branch review **APPROVE-WITH-FOLLOWUPS** (privacy verified: listed-only, allowlist, public, no leak; fixed 2 README inaccuracies). **The PARTICIPANT FLOW (PP-A authenticated sessions ✓ + PP-B invite links ✓ + PP-C my-data ✓ + PP-D catalogue ✓) IS COMPLETE.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-participant-pp-d*`; memory `project_participant_pp_d`. **NEXT (owner-approved sequence):** remaining Identity (ID-C2 GitHub-blocked / ID-C3 DataCite-blocked / ID-D Editor collaboration) OR Phase 5 Participant Platform proper; **Godot viewer LAST.** Prior:

**Earlier 2026-06-22 — PARTICIPANT FLOW — PP-C ("my data") BUILT + MERGED. The owner's 2026-06-22 product goal is now FULLY DELIVERED.** Third participant-flow slice (commit `dcaea17f`): a logged-in participant lists their own sessions and **downloads their own responses** (BDM CSV), strictly self-scoped. VS: `GET /v1/me/sessions` + `GET /v1/me/responses.csv` (require an Identity Bearer token; scope ONLY to `claims["sub"]` — id never from a param; reuse the export serializer + a participant-scoped iterator; own-connection CSV stream; invite participants excluded by namespacing). Web Viewer: a small **`MyDataApp` portal** on a new `mydata.html` Vite entry (shipped in prod) — log in → session list → "Download my data (CSV)"; reuses PP-A login; token in-memory only. VS 182 + web-viewer 239 + clean build; opus whole-branch review APPROVE (data-isolation verified — no cross-participant leak). **The PP track delivers the whole goal:** PP-A authenticated sessions (data tied to a returning id) ✓ + PP-B signed invite links (run via a link, no recovery) ✓ + PP-C "my data" (see/download own data) ✓; **PP-D (pick-a-questionnaire browse) is the only remaining convenience entry point.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-participant-pp-c*`; memory `project_participant_pp_c`. **NEXT = PP-D** (a participant catalogue/home), or other tracks (Phase 5 Participant Platform proper, Godot viewer last). Prior:

**Earlier 2026-06-22 — PARTICIPANT FLOW — PP-B (signed invite links) BUILT + MERGED.** Second slice of the participant-flow (PP) track (commit `05617521`): a researcher generates **tamper-proof per-participant invite links** for a deployment; opening one runs the questionnaire under that participant code — **no login, no account, no data recovery** (the agreed "(b)/(c)" mode; also the practical participant-onboarding path). **VS signs invites itself** (HMAC-SHA256, Identity stays frozen): `invites.py` mint/verify (constant-time, fail-closed on unset `INVITE_SIGNING_SECRET`, exp + deployment-bound); a new `invite_link` deployment mode; researcher-gated `POST /v1/deployments/{id}/invites` → `{invite_token, …, url}`; `POST /v1/sessions/new` accepts an `invite` and tags the session `participant_sub="invite:<code>"` (namespaced so it can't collide with an Identity sub) — **identity is not spoofable** (the code comes only from the verified payload). Three auth modes coexist (anonymous / `authenticated`(PP-A login) / `invite_link`). Web Viewer reads `?invite=` (no login screen; invalid/expired → a clear error). VS 174 tests + web-viewer 234 + clean build; opus whole-branch review APPROVE (security verified vs forgery/tamper/replay/fail-open/spoof). PP track: **PP-A ✓ → PP-B ✓ → PP-C (NEXT) "my data" download (account holders only) → PP-D pick-a-questionnaire.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-participant-pp-b*`; memory `project_participant_pp_b`. **NEXT = PP-C.** Prior:

**Earlier 2026-06-22 — PARTICIPANT FLOW STARTED — PP-A (authenticated participant sessions) BUILT + MERGED.** The owner gave a concrete product goal: participants pick + complete questionnaires with data tied to a **returning user id**, can come back + **download their own data**, plus **link-driven runs**. This is the **participant-flow (PP) track** (a focused Participant Platform built on Identity) — and is distinct from the deferred ID-C2 contribution workflow (researcher publishing, GitHub-blocked). PP decomposed: **PP-A authenticated sessions ✓** → PP-B signed invite links → PP-C "my data" download → PP-D pick-a-questionnaire. **PP-A (commit `57e413f0`):** a participant logs in (Identity email+password) in the Web Viewer and the Viewer Service tags the completion session with their stable Identity `sub` (returning participants accumulate sessions). VS: new `authenticated` deployment preset (`auth: identity`), `session.participant_sub` column, mint path that verifies the participant token (role-agnostic, reuses the ID-B verifier) → `participant_sub = agent_id = sub`, `session_index = count+1`, `401 auth_required` without a token; anonymous deployments unchanged. Web Viewer: `loginParticipant` + `LoginView` + a boot login-gate (`auth_required` mint → login → retry-with-Bearer → proceed); the Identity token is used only at mint. Identity model: (a) account login (return + my-data) + (b) signed invite links = "run without login / no data-recovery" (PP-B). Data home: always the platform DB (downloadable/exportable); external forwarding is a deferred per-deployment option. VS 155 tests + web-viewer 230 + clean build; opus whole-branch review APPROVE. **Deploy note:** the web-viewer needs `VITE_IDENTITY_BASE_URL` at build time for authenticated runs (anonymous unaffected). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-participant-pp-a*`; memory `project_participant_pp_a`. **NEXT = PP-B** (signed invite links). Prior:

**Earlier 2026-06-22 — IDENTITY ID-C1 BUILT + MERGED — Library community signals (its first write endpoints).** Third Identity slice (commit `3b96ed87`): the Library (`library/`) gains its first Identity wiring + write endpoints — **Identity-gated threaded comments + 1–5 ratings** in the Library's own Postgres, plus **GDPR self-erasure** (`DELETE /v1/me/community-data`). New `library/api/identity.py` (`require_user`/`require_admin`/`optional_user`); comments are single-level threaded with **soft-delete tombstones** that double as the GDPR PII-removal mechanism (preserve thread structure); ratings upsert one-per-user (mean/count/histogram, `my_score` only with a token); `author_sub` always from the verified token; catalogue **reads stay public/ungated**. 150 library tests; opus whole-branch review APPROVE. **⚠️ DEPLOY note:** the live Library auto-deploys from master via root `api/index.py`+`requirements.txt` (`pip install ./library`); since `library` now imports `identity_service` on boot, the root **`requirements.txt` now also lists `./identity-service`** (added + verified in a clean venv) — without it the live site would `ModuleNotFoundError`. ID-C decomposed: **ID-C1 community signals ✓** → **ID-C2 (NEXT) contribution write/review workflow** (drafts/in_review lifecycle + the editor's "Propose shared version" unblock — but the GitHub-PR-vs-DB-draft model is an OPEN design question the docs defer, so it needs its own careful brainstorm) → ID-C3 DOI minting. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-identity-id-c1*`; memory `project_identity_id_c1`. NEXT = **ID-C2**. Prior:

**Earlier 2026-06-21 — IDENTITY ID-B BUILT + MERGED — viewer-service control-plane now gated.** Second Identity slice (commit `e4fb3872`): the Viewer Service's **researcher/admin control-plane** (deployment CRUD, viewer registry, theme writes, runtime mint, `export.csv`, metrics, `DELETE /runtime_cache`) now requires an ID-A access token. New `viewer-service/src/viewer_service/api/identity.py` provides `require_researcher` (any of researcher/reviewer/administrator) + `require_admin` (administrator), verifying the Bearer JWT via ID-A's reusable `identity_service.identity_client` for audience `questionnaire-apps`; **fail-closed**, 401/403. `created_by` is taken from the token `sub` (not body-spoofable). The **participant path** (`/v1/sessions/*`, session-token-gated), `GET /v1/scorers/.../impl.wasm`, and `/healthz` stay **anonymous** by design. Config: `IDENTITY_JWKS_URL`/`IDENTITY_ISSUER`/`IDENTITY_AUDIENCE`. 146 VS tests green; built subagent-driven (6 TDD tasks + per-task review + opus whole-branch review = APPROVE). Deferred (viewer-service/FOLLOWUPS.md): per-record deployment ownership + `editor_session`/`platform_session` mint-time enforcement → ID-D/Phase 5. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-21-identity-id-b*`; memory `project_identity_id_b`. **NEXT = ID-C** (Library write/contribution + comments/ratings auth) — its own brainstorm→spec→plan→build cycle. Prior:

**Earlier 2026-06-21 — IDENTITY/AUTH STARTED — ID-A (core auth service) BUILT + MERGED.** The owner reframed "what's next" to **complete ALL remaining components, with the Native/Godot viewer LAST**, and flagged that the prior recommended-next list missed the components deferred behind the missing keystone: **Identity/Auth (OD-08)**. The design treats Identity as an external sibling project that doesn't exist, so nobody was building it. Decision: **build it in THIS repo now**, scoped to the questionnaire project but **architected to later stand alone and serve multiple Behaverse projects without a data migration** (audience-aware). Identity decomposed **ID-A..D**; **ID-A is COMPLETE** (commit `cedb5355`): a new standalone **`identity-service/`** (Python/FastAPI, mirrors `library/`+`viewer-service/`) issuing **EdDSA-JWT access tokens** (verified locally by consumers via `/.well-known/jwks.json`) + **opaque rotating refresh tokens** (reuse→family-revoke), **Argon2id** email+password accounts, **audience-scoped 5-role RBAC**, a reusable **`identity_client`** verifier (pyjwt+httpx only — ID-B+ import it to gate endpoints), stubbed `Mailer`, and a CLI (`identity migrate|generate-key|create-admin|create-client`). 45 tests; built subagent-driven (12 TDD tasks + per-task review + opus whole-branch review); deferrals in `identity-service/FOLLOWUPS.md`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-21-identity-id-a*`; memories `project_identity_id_a` + `project_identity_roadmap`. **The full remaining-component sequence (owner-approved): (1) Identity ID-A✓ → ID-B gate viewer-service → ID-C Library write/contribution+comments → ID-D Editor collab + "Propose shared" + real preview deploy; (2) Participant Platform (Phase 5, the biggest piece, consumes Identity); (3) Library Phase 6 (contribution/review + DOI + community signals); (4) Editor Identity-gated items; (5) unblocked anytime: behavioural-capture schemas (EEG/webcam/mic), external Scorer SDKs, editor a11y + deploy; (6) Native/Godot viewer LAST.** NEXT = **ID-B** (its own brainstorm→spec→plan→build cycle). Prior context (still valid):

**Earlier 2026-06-21 — EDITOR RESUMED + SUBSTANTIALLY EXTENDED (un-parked).** The 2026-06-16 "parked" note is superseded. Over 2026-06-17..21 the editor gained five new stages, all built subagent-driven (per-task review + Opus whole-slice review) and merged to `master`:
- **ED-H** — UX/visual refresh: design tokens → Tailwind `ed-*`, lucide icons, `Button/IconButton/Menu/Tabs` primitives, readable tree/canvas labels (prompt TEXT not raw refs), topbar clusters + `Export ▾` menu, Logic/Validation/Scoring as **Inspector tabs**, and a full re-token of every editor internal (zero `slate-*` left).
- **ED-I** — use-case docs + owner-feedback fixes (reuse-first +Add/Pick merge, read-only ref content, picker **content search**).
- **ED-J** — **translation**: machine **auto-translate** (per-row + bulk "Auto", status `draft`) via a serverless `editor/api/translate` proxy → Claude; (J2 DB-wide workbench — later folded into ED-K and removed).
- **ED-D4b** — **live score preview**: the web-viewer exports its scoring engine (`@behaverse/questionnaire-renderer/scoring`); the editor runs the bundled reference **PHQ-9** wasm so the ScoringPanel shows live values and `score()` is live in show_if/logic/validation. Loadable PHQ-9 sample added.
- **ED-K** — **Library Entity Browser** (the big one; K1+K2+K3, COMPLETE): a "Library entities" workspace to **browse → inspect → edit → translate** any Library entity (5 editable types via the standalone editors; inspect-only for the rest), with a local autosaved edit session and **full-body contribution export** (Library is read-only/OD-08 → export, never live-write). ED-K **retired and folded in** the standalone translate workbench.
- **Polish pass** — resolved the high-value FOLLOWUPS (status-control redundancy, batch-prefer-session-bodies, a few UX nits).

**All suites green: ~430 editor unit + ~24 e2e; typecheck clean; `npm run build` emits index.html + preview.html + wasm + samples.** The live editor state is tracked in the agent **memory index** (`.../memory/MEMORY.md`, kept current — the `project_editor_ed_*` entries are authoritative) and per-stage deferrals in **`editor/FOLLOWUPS.md`**. Specs/plans under `docs/superpowers/{specs,plans}/2026-06-1*-editor-*` + `2026-06-2*-editor-*`.

**⚠️ MULTI-AGENT REPO:** a separate **harvester** agent shares this same checkout and pushes `master` concurrently (it works under `questionnaire-harvester/` + `library/`). Keep your commits scoped to your area; `git fetch origin` + ff-or-rebase before every push; `scripts/seed-supabase.md` is perpetually-modified (not yours — stash it before a rebase). `HANDOFF.md` is gitignored (local doc); `editor/.env.local` is gitignored (holds the translate key).

**Auto-translate / running the editor locally:** `cd editor && npm run dev` (pinned to :5173 for live-Library CORS) now serves `/api/translate` via a dev shim — so **Auto buttons work without `vercel dev`** if `editor/.env.local` has a key. Provide **ONE** of `ANTHROPIC_API_KEY` (direct, no Vercel card) or `AI_GATEWAY_API_KEY` (Vercel AI Gateway, needs a card to release free credits); optional `TRANSLATE_MODEL` (default `anthropic/claude-haiku-4-5`; e.g. `claude-sonnet-4-6` for quality). See `editor/.env.example`. The editor is **not deployed**; deploying = static SPA + the one `/api/translate` function (needs the key as a Vercel env var).

**A NEW AGENT — recommended next, pick one (none blocking):** (1) **editor backlog polish** — the standout is **modal a11y** (`editor/FOLLOWUPS.md` ed-oo: `ForkDialog`/`LibraryPicker` need Escape-to-close, `role="dialog"`, focus-trap); (2) **deploy the editor** (Vercel: SPA + `/api/translate`); (3) **Phase 4 — Native/Godot Viewer** (`qv_godot/` scaffold exists; cross-viewer parity, OD-01 S1 — the biggest non-Identity-blocked piece); (4) **OD-08 Identity-gated** items (Library write-back/contribution + DOI, real Viewer-Service `preview` deploy, "Open in viewer", sharing translations to the Library) — unblock once the Identity sibling project exists. Prior editor status (for history):
**Editor ED-G (usability/onboarding pass) shipped + merged to master** (`6e31da36`), on top of the complete ED-A..F arc. ED-G adds NO new authoring capability — it's the owner-feedback UX pass that makes the editor approachable: a one-click **"Load a sample"** (self-contained offline **BIS/BAS** bundle, `editor/src/samples/bisbas.bundle.json`, frozen via `editor/scripts/build-sample.mjs` from the Library resolution-bundle; new `Source.sample`); a **browse-from-Library picker** (`LibraryQuestionnairePicker`, latest-version default) replacing the type-in id+version as the primary path; a **"← Home"** topbar button (wires the store's `reset()`, dirty-guarded, keeps the autosave); the label **"Option (Response)"**; a "Searches title & description" hint in both Library pickers (the server full-text index covers title+description only); and an entity-resolution **throttle (≤5 concurrent) + retry-once** (`editor/src/persistence/concurrency.ts` → `resolveEntities`/`fetchEntityBody`/`latestVersion`) that **de-flakes ref-based previews** (the boot-time fetch storm was serverless load-shedding that Chrome mis-reported as CORS errors — every Library endpoint is actually 200 + correct CORS per-request). 342 editor unit tests + an onboarding Playwright smoke + full e2e 17/17; typecheck clean; build emits index.html+preview.html+wasm and bundles the sample JSON. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-editor-ed-g*`; memory `project_editor_ed_g`; deferrals in `editor/FOLLOWUPS.md` ed-g-1..4 (server search-index expansion, frozen-sample refresh, keep-draft on Home, flat latest-only browse list). **The editor is now both feature-complete (ED-A..F) AND approachable (ED-G).** Prior:
**Editor ED-A..F COMPLETE — the entire planned editor arc is shipped + merged to master.** ED-F (the final stage) added a **standalone, no-backend shareable preview**: a reusable `PreviewView` (props-only, extracted from `PreviewPane`) rendered full-page by a 2nd Vite entry `preview.html`/`StandalonePreview` that loads an exported `{questionnaire,entities}` bundle (sessionStorage handoff from the Topbar "Open preview" button, or a file-open input), with live logic/validation, **no store and no network** (`makePoolFetcher(()=>bundle.entities, async()=>null)` → Library refs absent from the bundle render as placeholders). Real Viewer-Service `preview` deployment stays **OD-08-deferred** (FOLLOWUPS ed-f-1: VS rejects the `preview` preset; no Identity; VS mints from the Library, drafts aren't there). 331 editor unit tests + a standalone-preview Playwright smoke + the FULL e2e suite 16/16 (e2e-rot caught again — the new "Open preview" button made `/preview/i` ambiguous vs the "▢ Preview" toggle → 7 specs rescoped to the exact `▢ Preview` name); typecheck clean; build emits both `dist/index.html` + `dist/preview.html` + the wasm. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-editor-ed-f*`; memory `project_editor_ed_f`. **NO remaining planned editor stages.** Opt-in follow-ons only (if owner directs): ED-D4b (live score preview), ED-E2 (side-by-side translation matrix), page/section/block + metadata-title translations (schema gap), validation-message localization (upstream schema decision), Logic/Validation/Scoring → Inspector tabs consolidation, single-file offline preview (ed-f-2). Standing note: the live Library auto-deploys from master pushes. Prior:
**Editor ED-A + ED-B + ED-C1 + ED-C2(a+b) + ED-C3(a+b) shipped** (the editor authors from scratch, picks Library entities with hard-pinned refs, AND flags/upgrades stale Library refs per OD-06; ED-C: C1✓ C2a✓ C2b✓ C3a✓ C3b✓ C4⬜. NEXT = ED-C4 OD-05 fork. Live Library auto-deploys from master pushes — the C3a entity-body endpoint is confirmed live (2026-06-15). tests: ED-A 50/ED-B 69/ED-C1 94/ED-C2a 112/ED-C2b 125/ED-C3a 133/ED-C3b 143 editor + 128 library). Prior:
**Editor ED-A + ED-B + ED-C1 + ED-C2(a+b) + ED-C3a shipped** (the editor authors from scratch AND picks existing entities from the Library with hard-pinned refs; ED-C3a also added the Library per-entity body endpoint + fixed ED-B's Library-ref preview — ⚠️ live Library needs a redeploy. ED-C: C1✓ C2a✓ C2b✓ C3a✓ C3b⬜ C4⬜. NEXT = ED-C3b OD-06 upgrade. tests: ED-A 50/ED-B 69/ED-C1 94/ED-C2a 112/ED-C2b 125/ED-C3a 133 editor + 128 library). Prior:
**Editor ED-A + ED-B + ED-C1 + ED-C2 (C2a+C2b) shipped** (the editor authors brand-new questionnaires from scratch — items/prompts/options/context/instruction/messages — with live preview + bundle export; ED-A 50 / ED-B 69 / ED-C1 94 / ED-C2a 112 / ED-C2b 125 vitest tests, each with Playwright smokes. ED-C: C1✓ C2a✓ C2b✓ C3⬜ C4⬜. NEXT = ED-C3 pick-from-Library + hard-pin — see the ACTIVE TASK brief). Prior:
**Editor ED-A + ED-B + ED-C1 + ED-C2a shipped** (decomposed ED-A..F; ED-A shell+model+tree; ED-B inline preview; ED-C sliced C1..C4 with C2 sub-sliced C2a/C2b — **C1 Option editor** + **C2a entity pool + Prompt editor + new-item creation** done. All merged to master under `editor/`; ED-A 50 / ED-B 69 / ED-C1 94 / ED-C2a 112 vitest tests, each with Playwright smokes. The editor can now author brand-new items from scratch. NEXT = ED-C2b = Context/Instruction + Message authoring — see the ACTIVE TASK brief). Earlier 2026-06-14: **Phase 1 shipped; the Web Viewer is feature-complete AND polished; in-session scoring works end-to-end.** Since the WV-A..F completion (the full Web-Viewer summary is the "Web Viewer — START HERE" brief lower down): **Visual Polish Stage 1** (a data-driven theme system + dev gallery + Minimal/Sage/Artsy themes; authoring runbook `web-viewer/THEMES.md`) and **OD-16 scoring SP1 + SP2a + SP2b** are all built + merged. A participant's answers now compute **real scores** via a reference WASM Scorer (`questionnaire-scorer/`); score-gated **branching fires live** (SP2a); scores **display** at terminal + live and **`scorer_outputs` persist** to the Viewer Service (SP2b). **NEXT ACTIVE TASK = the Editor** — the custom React/TS authoring UI. See the "▶ ACTIVE TASK — The Editor" brief immediately below. **All suites green:** web-viewer 213, viewer-service 134, denormaliser 57, library 126, library-web 59, schema 309 (44 examples), expr 24 (Rust)+31 (WASM), scorer 9 (cargo)+6 (host). (8 data schemas in-repo, v26.0609; public hosting at behaverse.org deferred; ALL durations in SECONDS; expression grammar normative in design/15.)

---

## EDITOR — ACTIVE again (resumed 2026-06-17; see the dated lead at the top for the 2026-06-17..21 work)

**Status note (was "parked 2026-06-16"):** the owner re-prioritized the editor on 2026-06-17 and it was substantially extended (ED-H, ED-I, ED-J, ED-D4b, ED-K, polish — see the **Last updated: 2026-06-21** lead at the top). The text below is the **2026-06-16 snapshot** kept for history; the authoritative current state is the top lead + the `project_editor_ed_*` memories + `editor/FOLLOWUPS.md`.

**What the editor does today (all merged to master, all suites green):**
- **ED-A..F** (the full planned arc): app shell + canonical Schema-2 model + structure tree; inline WYSIWYG preview via the Web-Viewer renderer lib; item/Question/Option authoring + the OD-15 reusable-entity workflow (create-inline, pick-from-Library with hard-pinned `@vYY.MMDD` refs, OD-06 upgrade, OD-05 fork); logic/validation/scoring builders with live WASM-evaluator checks; translation (per-locale content); standalone no-backend shareable preview + export (JSON + self-contained bundle).
- **ED-G** usability pass: "Load a sample" (offline BIS/BAS), browse-from-Library picker, "← Home", consistent buttons, search-scope hints, fetch throttle+retry.
- **Polish rounds:** content renders as **inline markdown + sanitized HTML** (in the shared renderer — participant-facing too); compact preview scaling; resizable panels (persisted widths); numbered tree + dropped "inline"; tooltips; "Preview" header; reworded editing-locale badge; questionnaire-picker browse+filter; open-with-page_main + preview-on + "Questionnaire" root node + picker autofocus; window-scroll/topbar fix; green/amber translated-dots in the tree.
- **Translation panel (ED-E2):** full-width source→target view of every translatable string, deduped by entity, auto-forking Library refs on edit (shared options forked once), per-row status + progress + untranslated-only filter.

Memories carry the detail: `project_editor_ed_a..g`, `project_editor_translation_panel`, `project_renderer_markdown_preview_polish`. Specs/plans under `docs/superpowers/{specs,plans}/2026-06-1*-editor-*`. Per-stage deferrals in `editor/FOLLOWUPS.md`.

**Deferred editor backlog (only if the owner asks):**
- **Auto-translate button** (machine-translate the target column / a row / the whole questionnaire) — explicitly deferred 2026-06-16 (`editor/FOLLOWUPS.md` ed-defer-1).
- ED-D4b live score preview (needs a bundled scorer wasm + the SP2a executor ported into the editor).
- Translate **placeholder/help** text + **page/section/block + metadata titles** (`translations[locale]` field / schema gap; some are upstream schema decisions).
- Validation-message / metadata-title **localization** (schema gap — plain strings in Schema 2; upstream owner decision).
- Consolidate Logic/Validation/Scoring into **Inspector tabs**; single-file **offline preview**; broader **visual restyle** beyond the alignment pass.
- **OD-08-gated:** the editor's **"Open in viewer"** real Viewer-Service `preview` deployment, and **writing forked/translated entities back to the shared Library** (so translations are shared across questionnaires) — both need Identity.

**Original brief (for history).** Build the Editor producing canonical **Schema 2** JSON; authoritative design **[design/07_editor.md](design/07_editor.md)** (11 capability areas); stack OD-01 (custom React+TS, NO SurveyJS); under `editor/` (mirrors `library-web/`'s Vite+React19+TS+Tailwind; migrates to `questionnaire-editor` at the deferred repo split).

> **PROGRESS (2026-06-16): the Editor was decomposed into ED-A..F; ED-A, ED-B, ALL of ED-C, ALL of ED-D's authoring surface, and now ED-E (translation) are BUILT + MERGED to master.** **ED-E** unlocked per-locale content editing by reusing the existing (already `locale`-parameterized) content editors: a store `editingLocale` + a topbar **"Editing language" switcher** that drives `ItemEditor`/`MessagePane` to edit `editingLocale ?? primary` (a missing `content[locale]` auto-creates on first edit); an Inspector **`LanguagesField`** to manage `metadata.available_languages` (primary non-removable); per-locale **`status`** (draft/complete/validated) + a read-only **source-text hint** in the content editors. It translates the whole `content`-based surface (prompts/options/contexts/instructions/messages/placeholders/help); the editing locale is independent of the preview's language picker. **Validation-message + metadata-title localization is a SCHEMA GAP** (those are plain strings in Schema 2, not language-keyed — design §5 calls messages "translatable", so making them per-locale is an **upstream schema decision for the owner**, deferred). Also: ED-E **caught + fixed e2e rot** — older Playwright specs (logic-rule, piping, 3 smoke) had silently broken on master because successive stages added more global panels/modals (a 2nd "+ Add rule" from the Validation panel, the LibraryPicker "Insert" button) creating strict-mode selector ambiguities, and each stage only ran its OWN new e2e; ED-E scoped the selectors to their panel/modal and **the full `npm run e2e` suite is green again** (lesson recorded: re-run the full e2e suite after adding any global panel/modal, not just the new spec). 322 editor tests + full e2e green; production build emits the wasm. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-e*`; memory `project_editor_ed_e`. **NEXT editor stage = ED-F** (preview deployment + export) — the last stage in the planned ED-A..F arc. Deferred follow-ons: **ED-D4b** (live score preview — bundle a reference-scorer wasm + port the executor), **ED-E2** (side-by-side translation matrix), page/section/block title `translations`, and consolidating the Logic/Validation/Scoring global panels into Inspector tabs. **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: ED-A, ED-B, ALL of ED-C, and ALL of ED-D's AUTHORING surface (D1 visibility + D2 logic + D3 validation + D4 scoring) BUILT + MERGED to master. **ED-D4** (the scoring builder) shipped **author-only**: typed `Questionnaire.scores[]` (OD-16 `Score`: `id` + Library `scorer` ref + JSON-Pointer `path` + name/description) + `updateScores` + a `parseRef` `scr_` prefix; a questionnaire-global **"Scores" panel** below Logic + Validation in the root Inspector doing CRUD (auto-`score_N` id, scorer via manual entry **or** the Library picker, JSON-Pointer path, name/description, per-field validation mirroring the canonical schema). All **three global panels (Logic / Validation / Scores) now coexist** in the root Inspector (distinct labels, no collisions). The panel honestly notes "scores are computed by the deployed viewer — not shown live in this preview." 308 editor tests + a passing Playwright scoring smoke; production build emits the wasm. **The live score preview was DELIBERATELY DEFERRED (owner-approved)**: the editor has no Viewer Service to serve scorer wasm, no denormaliser to pin `impl`, the live Library has zero Scorer entities seeded, and a bundled reference scorer still couldn't run *custom* scorers — so `score()` stays null in preview (score-referencing logic/validation conditions remain inert in preview, as before). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-d4*`; memory `project_editor_ed_d4`. **The ED-D *authoring* surface is COMPLETE.** **NEXT editor stage = ED-D4b (live score preview)** — bundle a reference-scorer wasm (prepare step like the evaluator) + port the web-viewer's SP2a score executor (`web-viewer/src/scoring/`: input-assembly + sha256-verify + compile + JSON-Pointer + cache) + a minimal impl-pin, wiring `score(id)` into the preview bindings (reference-scorers-only; also lights up score-referencing logic/validation conditions) — **OR ED-E (translation)**, then ED-F (preview-deploy + export). With three global panels now in the root Inspector, **consolidating Logic/Validation/Scoring into Inspector tabs** is a worthwhile deferred refinement (FOLLOWUP ttt). **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: ED-A, ED-B, ALL of ED-C, ED-D1, ALL of ED-D2, and ALL of ED-D3 (D3a + D3b) BUILT + MERGED — ED-D3 (validation) COMPLETE. **ED-D3b** (the final D3 slice) added **cross-question validation** — a questionnaire-global "Validation rules" panel below Logic in the root Inspector doing CRUD on `Questionnaire.validation[]` (`CrossQuestionValidationRule`: auto-`val_N` `id` + condition via `ExpressionInput` + `message` + a `targets` multi-select, with per-field validation: id/condition/message errors, duplicate-id/unknown-target/empty-targets warnings). The inline preview now shows **cross-question errors live** on their targets (`collectCrossQuestionErrors`, a faithful viewer port + the editor's malformed-safe `ev.check` guard), merged with D3a's per-question errors into the renderer's `requiredErrors`/`errorMessages`. Built as a clean clone of the proven D2a Logic pattern (distinct "Edit validation rule N" labels + "Validation rules" heading avoid query collisions). Scores referenced in cross-question conditions resolve `null` in the preview until ED-D4. 286 editor tests + a passing Playwright cross-validation smoke; production build emits the wasm. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-d3b*`; memory `project_editor_ed_d3b`. **The validation builder is now feature-complete** (per-question + cross-question both authored + displayed live). **NEXT editor stage = ED-D4** (scoring builders: a `scores[]` editor — pick a Scorer entity from the Library + a JSON-Pointer `path` into its `output_schema` [OD-16], with a live score preview that runs the reference Scorer WASM, like the web-viewer's SP2a `ScorerExecutor`). Then ED-E (translation), ED-F (preview-deploy + export). When ED-D4 adds its panel, consolidate Logic/Validation/Scoring into Inspector tabs (currently all are sections in the questionnaire-root Inspector). **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: ED-A, ED-B, ALL of ED-C, ED-D1, ALL of ED-D2, and ED-D3a BUILT + MERGED to master. **ED-D3** (validation) was split into **D3a** (per-question — DONE) and **D3b** (cross-question — NEXT). **ED-D3a** added a type-gated **"Validation" section** to the Option editor (`option.validation`: number→`range`, text→`length`[integer-coerced]+`format`, + their messages) — kept **distinct** from the existing `input_validation` regex (Schema 2 `OptionBase` genuinely carries BOTH; the viewer validates `validation`, so `input_validation` was just relabeled "Input mask (RegEx)"). It also made the inline preview **show per-question validation errors live**: a pure `logic/validation.ts` ports the viewer's `perQuestion` line-for-line (empty-skip, nullable open bounds, invalid-regex-passes, type-gated, default-message fallbacks) + `collectPerQuestionErrors` keys items exactly as the renderer (`elementKey`/`sectionChildFallback`, reading `el.option.validation`), and `PreviewPane` feeds the renderer's `requiredErrors`/`errorMessages` (replacing the hardcoded `[]`). Display-only + live (empty values never error — that's the separate ED-C4 `required` flag); no blocking submit gate. 257 editor tests + a passing Playwright validation smoke; production build emits the wasm. A review-caught Minor was fixed pre-merge (length bounds coerced to integers per Schema-2). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-d3a*`; memory `project_editor_ed_d3a`. **NEXT editor stage = ED-D3b** (cross-question validation: a questionnaire-global "Validation rules" panel mirroring the LogicPanel — `Questionnaire.validation[]`: `id` + condition via `ExpressionInput` + `message` + `targets` multi-select — extending `collectPerQuestionErrors` with cross-question rules in the preview). Then ED-D4 (scoring: `scores[]` Scorer-picker + JSON-Pointer path + live score preview), ED-E (translation), ED-F (preview-deploy + export). When D3b/D4 add their global panels, consolidate Logic/Validation/Scoring into Inspector tabs. **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: ED-A, ED-B, ALL of ED-C, ED-D1, and ALL of ED-D2 (D2a + D2b) BUILT + MERGED — ED-D2 (logic rules) COMPLETE. **ED-D2b** (the final D2 slice) added **piping authoring** — a `source` question dropdown + a `field_path` prompt-target **picker** (`collectPipingTargets` → the canonical `pages.{id}.elements.{idx}.prompt`, 0-based index verified == the viewer's `trialIndex-1`; `validateRule` gained a 3rd `pipingPaths` arg) — with **live same-page piping in the inline preview** (`applyPiping`, a faithful port of the web-viewer's app-layer prompt rewrite, composed **pipe-then-filter** so the `field_path` element indices stay stable through visibility filtering). Piping targets are scoped to **question prompts** — the only target the deployed viewer actually applies, so authoring others would silently not fire. It also added the **randomization checkboxes** (`Page`/`Section`/`Block.randomize` + `flow.randomize_pages` via a new `updateFlow` helper + `CheckboxField`) — author-only (the preview can't shuffle without a seeded runtime; unchecking removes the key, no `false` litter). 238 editor tests + a passing Playwright piping smoke; production build emits the wasm. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-d2b*`; memory `project_editor_ed_d2b`. **The logic builder is now feature-complete** (skip/branch/visibility/piping authored; visibility + piping execute in preview; randomization authored). **NEXT editor stage = ED-D3** (validation builders: per-question `Option.validation` [required/format/range/length + messages] + cross-question `Questionnaire.validation[]` [condition + message + targets], with live evaluation + error display in the preview), then ED-D4 (scoring: `scores[]` Scorer-picker + JSON-Pointer path + live score preview), ED-E (translation), ED-F (preview-deploy + export). When D3/D4 add their own questionnaire-global panels, consolidate Logic/Validation/Scoring into tabs (currently Logic is a section in the root Inspector). **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: ED-A, ED-B, ALL of ED-C, ED-D1, and ED-D2a BUILT + MERGED to master. **ED-D2** (logic rules) was sliced into **D2a** (navigation & visibility rules — DONE) and **D2b** (piping + randomization — NEXT). **ED-D2a** types `Questionnaire.logic[]` as `LogicRule[]` (+ `updateLogic` Immer helper), adds pure helpers `targets.ts` (page-id / element-key catalogues for rule targets) + `ruleOps.ts` (`newRule`/`summarizeRule`/`validateRule` — the per-type semantics the schema's loose `action` bag doesn't enforce), and a **Logic Rules panel** in the questionnaire-root Inspector doing full CRUD on **skip / branch / visibility** rules (condition via the D1 `ExpressionInput`, type-specific target dropdowns, inline error/warning validation). The inline preview now **executes visibility rules live** (precedence: matching visibility rule → element `show_if` → default-visible, mirroring the web-viewer's `isElementVisible`); `isElementShown`/`filterPageVisible` gained an optional `rules: LogicRule[] = []` param so all D1 callers stay green. **Skip/branch are author+validate-only** — the editor preview has no page-to-page navigation runtime, so they're labelled "runs in the deployed viewer". Logic panel home = the root Inspector section (consolidate Logic/Validation/Scoring into tabs once D3/D4 land). 211 editor tests + a passing Playwright visibility-rule smoke; production build emits the wasm. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-d2a*`; memory `project_editor_ed_d2a`. **NEXT editor stage = ED-D2b** (piping authoring: `source` question dropdown + `field_path` prompt-target picker + live same-page piping in the preview; plus the randomization checkboxes `Page/Section/Block.randomize` + `flow.randomize_pages` — note option-order randomization isn't in Schema 2). Then ED-D3 (validation), ED-D4 (scoring), ED-E (translation), ED-F (preview-deploy + export). **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: ED-A, ED-B, ALL of ED-C, and ED-D1 BUILT + MERGED to master. **ED-D** (logic/validation/scoring builders) was decomposed with the owner into **D1** (expression foundation + visibility — DONE), **D2** (logic rules: skip/branch/piping/randomization + `logic[]`), **D3** (validation: per-question `Option.validation` + cross-question `Questionnaire.validation[]`), **D4** (scoring: `scores[]` Scorer-picker + JSON-Pointer path + live score preview). Authoring philosophy = **expression-first + thin form helpers**. **ED-D1** embedded the OD-11 WASM expression evaluator into the editor (reusing the web-viewer's WV-D approach: static-string dynamic import, gitignored `editor/src/logic/wasm/`, build chained into `predev`/`prebuild`); added a reusable **CONTROLLED** `ExpressionInput` (live `check_expression` ✓/✗ syntax + warning-grade ⚠ unknown-ref detection + an "insert condition" `[question][op][value]` helper) backed by an id catalogue + ref-checker; wired per-element `show_if` set/clear into the **Inspector** for page/section/block/item (keyed by node path so drafts don't leak between selections); and made the inline preview **hide/show elements live** as throwaway answers change (reads `show_if` directly off elements — the key-based program map + visibility *rules* are D2). A malformed `show_if` is **false-safe to SHOWN**, matching the web-viewer. Partially closes the old ED-B FOLLOWUP (i) (visibility only). 178 editor tests + a passing Playwright `show_if` smoke; production build emits the wasm asset. Two review-caught bugs were fixed before merge (a subagent's uncontrolled-textarea workaround → reverted to controlled + precise test query; a missing React `key` on `ShowIfEditor` → fixed + regression-tested). Schema-2 gotchas surfaced: page-element `id` must match `^it_[a-z0-9_]+$` and **prompts are ref-only (OD-15)** — inline prompt *content* is invalid (inline options are fine); also the editor's file-open loads a plain questionnaire only (does NOT populate the pool from a `{questionnaire, entities}` bundle — a real gap). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-d1*`; memory `project_editor_ed_d1`. **NEXT editor stage = ED-D2** (logic rules: structured skip/branch/piping/randomization + the questionnaire `logic[]` panel + visibility LogicRules + their preview execution), then D3 (validation), D4 (scoring), then ED-E (translation), ED-F (preview-deploy + export). **Standing note:** the live Library auto-deploys from `master` pushes.
>
> Earlier 2026-06-15: **ED-A, ED-B, and ALL of ED-C (C1, C2a, C2b, C3a, C3b, C4) are BUILT + MERGED to master — ED-C is COMPLETE.** The editor now **authors questionnaires from scratch, reuses Library content with hard-pinned refs, keeps refs current (OD-06 upgrade), and forks Library content to edit locally (OD-05).** **ED-C4** (final ED-C slice) made Library-pinned refs forkable: `upgradeRef`→`repointRef`; a fork store slice + `forkRefAction` (fetch body → pool copy `<id>@<pinnedVer>.dev1` → repoint-all → clear staleness); a `ForkDialog` (Derive locally / Propose-shared DISABLED per OD-08 / Cancel) opened via "Fork to edit" on every read-only Library-ref chip → the forked ref becomes editable via the pool editors; plus the OD-05 `required` override checkbox on item rows. Never writes the Library. 151 editor tests + a Playwright fork smoke. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-editor-ed-c4*`; memory `project_editor_ed_c4`. **NEXT editor stage = ED-D** (logic/validation/scoring builders — structured skip/visibility/piping/branching/randomization + per/cross-question validation + `scores[]` per OD-16; live checks via the WASM expression evaluator [`@behaverse/expression-evaluator`, OD-11] + the reference Scorer [`questionnaire-scorer/`]). Then ED-E (translation), ED-F (preview deployment + export). **Standing note:** the live Library auto-deploys from `master` pushes (the C3a entity-body endpoint is confirmed live).
>
> Earlier 2026-06-15: **ED-A, ED-B, ED-C1, ED-C2(a+b), and ED-C3(a+b) are BUILT + MERGED to master (ED-C3 COMPLETE).** **ED-C3b** implements OD-06's never-silent freshness for Library-pinned refs: on model load + a topbar **"Check for updates"**, it fetches each pinned entity's latest published version (`/v1/entities/{etype}/{eid}`), flags refs where latest > pinned (CalVer), and surfaces a per-chip **"newer: vX [Upgrade]"** badge + a topbar **"⬆ N updates"** count; **Upgrade** re-points all occurrences explicitly (never auto). Pure staleness helpers + `latestVersion` client + `upgradeRef` replace-all + a store `staleness` slice (deduped/race-free refresh) + `UpgradeBadge` on all ref chips. Offline → nothing flagged (no false positives). 143 editor tests + a Playwright upgrade smoke. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-editor-ed-c3b*`; memory `project_editor_ed_c3b`. **ED-C status: C1✓ C2a✓ C2b✓ C3a✓ C3b✓ C4⬜. NEXT = ED-C4** (OD-05 override/fork: editable position/required/show_if on refs; content edit → fork dialog → derive-locally into the pool; propose-shared is OD-08-blocked). **Live Library: the `/v1/entities/.../definition` body endpoint is CONFIRMED LIVE (verified 2026-06-15)** — the Vercel project auto-deploys from `master` pushes, so the C3a merge shipped it; pick-from-Library / ED-B Library-ref preview / C3b `latestVersion` all work against the live API. No manual redeploy needed (future Library API changes deploy on push).
>
> Earlier 2026-06-14: **ED-A, ED-B, ED-C1, ED-C2(a+b), and ED-C3a are BUILT + MERGED to master.** **ED-C3a** (first slice of ED-C3, pick-from-Library) spans `library/`+`editor/`: it adds the missing Library **per-entity body endpoint** `GET /v1/entities/{etype}/{eid}/versions/{version}/definition` and **repoints ED-B's `fetchEntityBody`** at it — fixing a real ED-B bug (Library-pinned refs were silently previewing as placeholders; ED-B FOLLOWUPS g). A modal **`LibraryPicker`** (search + body snippet) opens via a store slice from per-slot "Pick from Library" buttons (prompt/option/context/instruction) + Canvas "+ Pick item"/"+ Pick message"; selecting inserts a **hard-pinned `@vYY.MMDD` ref** (latest), read-only ("fork to edit (ED-C4)"), previewing live. 128 library + 133 editor tests + a Playwright pick smoke. **⚠️ ACTION: the live Vercel Library (questionnaire-library.vercel.app) must be REDEPLOYED to expose the new `/definition` endpoint before pick-from-Library / ED-B Library-ref preview work against the live API** (editor tests + smoke stub it). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-editor-ed-c3a*`; memory `project_editor_ed_c3a`. **ED-C status: C1✓ C2a✓ C2b✓ C3a✓ C3b⬜ C4⬜. NEXT = ED-C3b** (OD-06 newer-version notification + explicit upgrade), then C4 (OD-05 fork).
>
> Earlier 2026-06-14: **ED-A, ED-B, ED-C1, ED-C2a, and ED-C2b are BUILT + MERGED to master (ED-C2 COMPLETE).** **ED-C2b** adds authoring of the auxiliary content entities (completing the from-scratch authoring ED-C2a started with Prompts): a reusable `ContentTextEditor` + Context/Instruction(+`dimension`)/Message editors; pure minters + `tree.unsetNodeProp`; **ItemEditor Context + Instruction** add/edit/remove sections (mint a draft pool entity, set/unset the `question` ref, drop the pool entity on remove); **Canvas "+ Add message" + `MessagePane`** (all message selections routed; Library-pinned refs get a "fork to edit (ED-C4)" note). Everything is a draft pool entity that live-previews and rides the `{questionnaire,entities}` bundle export. 125 vitest tests + a Playwright context+message smoke. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-editor-ed-c2b*`; memory `project_editor_ed_c2b`. **ED-C status: C1✓ C2a✓ C2b✓ C3⬜ C4⬜. NEXT = ED-C3** (pick-from-Library: embedded read-only Library browser via the live `/v1/entities`+`/v1/search` API; selecting inserts a hard-pinned `@vYY.MMDD` ref; OD-06 newer-version notification + explicit upgrade), then C4 (OD-05 override/fork).
>
> Earlier 2026-06-14: **ED-A, ED-B, ED-C1, and ED-C2a are BUILT + MERGED to master.** ED-C is sliced C1..C4; **C2** is sub-sliced **C2a (DONE) + C2b**. **ED-C2a** makes the editor able to **author brand-new content from scratch**: an editor-local **entity pool** (store slice + draft persistence + `.devN` minting derived from `metadata.version` + `{questionnaire,entities}` **bundle export** + **pool-first live preview** that re-resolves on pool edits), a **PromptEditor** (per-locale text + name/construct/dimension/topics/reversed), and **"+ Add item"** (mint a draft Prompt into the pool + a default inline Option → inline item → edit + preview live). 112 vitest tests + a Playwright new-item smoke. Foundation rationale: Schema-2 Prompt/Context/Instruction are **ref-only** + Library write is OD-08-blocked, so new/forked content lives in the local pool with `.devN` drafts (the `^x_`-free version-regex affordance). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-editor-ed-c2a*`; memory `project_editor_ed_c2a`. **NEXT = ED-C2b** (Context/Instruction on a Question + Message authoring), then C3 (pick-from-Library + hard-pin OD-06), C4 (OD-05 fork).
>
> Earlier 2026-06-14: **ED-A, ED-B, and ED-C1 are BUILT + MERGED to master.** ED-C (item/Question/Option authoring + the OD-15 reusable-entity workflow) was itself sliced **ED-C1..C4** with the owner: **C1 = inline Option editor (DONE)**; C2 = local entity pool + Prompt/Question authoring + new-item creation; C3 = pick-from-Library + hard-pin (OD-06); C4 = OD-05 override/fork. **ED-C1** ships a type-aware Option editor (in a canvas `ItemEditor` for inline items + section `shared_option`): pure canonical-preserving `editor/src/option/ops.ts` (structural↔content choice-index alignment invariant), the type triple + `min/max_selected` + choice rows + numeric/text sub-editors + inline placeholder/help + a derived-widget hint (via the renderer's `deriveWidget`); edits round-trip Schema-2-valid and the ED-B preview reflects live. 94 vitest tests + a Playwright Option-editor smoke. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-editor-ed-c1*`; memory `project_editor_ed_c1`. **Owner-approved ED-C foundation (lands in C2): an editor-local entity pool with `.devN` draft versions + a self-contained `{questionnaire, entities}` bundle export** — because Schema-2 Prompt/Context/Instruction are **ref-only** (no inline-content form) and Library write is OD-08-blocked, so new/forked content needs a local home. **NEXT = ED-C2.**
>
> Earlier 2026-06-14: **the Editor was decomposed with the owner into ED-A..F; ED-A AND ED-B are BUILT + MERGED to master.** ED-B (inline WYSIWYG preview) ships the live split-pane preview using the Web Viewer renderer library directly (OD-03, consumed from `web-viewer/dist-lib` via a Vite alias): a thin TS resolve-and-shape projection (`editor/src/preview/`; a faithful port of the denormaliser's `resolve_refs` + Runtime shaping — only 2 of the denormaliser's 6 passes are needed because the renderer is presentational) + transitive cached Library entity-body resolution; a `PreviewPane` with language/device-frame/scope pickers + throwaway answers; the topbar `▢ Preview` toggle + a 4-col workspace split. **Static structural only** — logic/show_if/branching/scoring/validation are deferred to ED-D. 69 vitest tests + a passing Playwright preview smoke. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-editor-ed-b*`; memory `project_editor_ed_b`. **NEXT = ED-C** (item / Question / Option authoring + the OD-15 reusable-entity workflow: embedded pick-from-Library browser, create-inline, OD-06 hard-pin `@vYY.MMDD`, OD-05 override/fork). One open verification (FOLLOWUPS g): the Library entity-body endpoint `GET /v1/entities/{type}/{id}?version=` is assumed, not yet confirmed against the live API.
>
> Earlier 2026-06-14: **PROGRESS — the Editor was decomposed with the owner into ED-A..F, and ED-A is BUILT + MERGED to master.** ED-A (`editor/`, static React19+TS+Tailwind SPA, no backend) ships: open/create/load/save canonical Schema 2 with refs intact; browser-local persistence (IndexedDB autosave + file open/export + open-from-Library read API); the pure-TS model layer (types/Ajv-validation/serialize/path-addressing/Immer tree ops); the agreed **3-pane "preview-split" shell** (Topbar w/ Validate + disabled Preview placeholder + Export · left StructureTree with dnd-kit reorder + block grouping · center Canvas · right type-aware Inspector); Schema-2-valid round-trip gate. 50 vitest tests + a passing Playwright chromium smoke. Spec: `docs/superpowers/specs/2026-06-14-editor-ed-a-design.md`; plan: `docs/superpowers/plans/2026-06-14-editor-ed-a.md`; build details + gotchas: memory `project_editor_ed_a`; deferred items: `editor/FOLLOWUPS.md`. **NEXT = ED-B (inline WYSIWYG preview)** — embed the Web Viewer renderer library (`@behaverse/questionnaire-renderer` from `web-viewer/ npm run build:lib`); the real design question is how the browser denormalises a draft to a Schema-3 runtime for the renderer (port a JS denormaliser / wasm / small service). Start ED-B with its own brainstorm→spec→plan cycle. The remaining ED-A..F slices are unchanged below.

**This is a BIG component — decompose it first.** Like the Viewer Service (VS-A..E) and the Web Viewer (WV-A..F), do NOT try to build it in one spec. **Start by brainstorming a decomposition WITH the owner** (each sub-stage gets its own spec → plan → subagent-driven TDD build → review → merge). A natural slicing (confirm/adjust with the owner):
1. **App shell + canonical-JSON model + the structure tree** — open/create/load a Schema-2 questionnaire; the five-concept structural tree (Block ▸ Page ▸ Section ▸ Question, per OD-12 + memory `project_pagination_model`) in a left rail; a main canvas + right-hand property panels; save = a versioned snapshot of the canonical JSON. (Schema-2-valid round-trip is the gate.)
2. **Inline WYSIWYG preview** — a split-pane preview that renders the in-progress questionnaire using the **Web Viewer's renderer library** directly (`@behaverse/questionnaire-renderer`, OD-03 — see "building blocks" below). The preview IS the deployed renderer; no separate engine. Needs the questionnaire denormalised to a Schema-3-ish runtime for the renderer — reuse the **shared denormaliser** (OD-18; `questionnaire-runtime-denormaliser`, currently Python — decide: port a JS denormaliser, call a small service, or wasm — a real design question for this stage).
3. **Item / Option / Question authoring + the reusable-entity workflow** — type-aware question editor; the OD-15 entity model (Message, Context, Instruction, Prompt, Option, Placeholder, Help, RegEx, Question, Item, Solution, Scorer, Subscale — design/05a); **pick-from-Library** (embedded Library browser, read-only API is live) vs **create-inline**; **hard-pinning** `@vYY.MMDD` (OD-06) + the **reference override** surface (OD-05: only `position`/`required`/`show_if` overridable; any content edit opens a fork dialog).
4. **Logic + validation + scoring builders** — structured builders for skip / visibility / piping / branching / randomization + per-question & cross-question validation + the `scores[]` declaration (OD-16 external Scorer model). A **live evaluator/score preview** uses the **same WASM evaluator** the viewer embeds (`@behaverse/expression-evaluator`, OD-11) + the **reference Scorer** (`questionnaire-scorer/`) so what the Editor shows equals what the viewer computes.
5. **Translation interface** — side-by-side source/target, one row per translatable text element, per-row status, completeness indicator (the canonical `content` is a language-keyed map per OD-15).
6. **Preview deployment + export** — "Open in viewer" creates a **Preview deployment** in the Viewer Service (preset `preview`, ephemeral, short TTL — the VS deployment API is live); export canonical JSON / PDF / printable summary.

**Building blocks already BUILT and ready to consume (don't rebuild these):**
- **The renderer library (OD-03)** — `web-viewer/`'s `npm run build:lib` → `dist-lib/` (`@behaverse/questionnaire-renderer`: ESM + dts + precompiled CSS, React externalized). This is the Editor's inline-preview engine. (Local-path consume now; npm publish at the deferred repo split.) Themeable via the `--qv-*` tokens / `web-viewer/THEMES.md`.
- **The WASM expression evaluator (OD-11)** — `questionnaire-expression-evaluator/` → `@behaverse/expression-evaluator` (`evaluate_condition`/`check_expression`/`reversed`/`compare`). Use it to validate authored expressions + power live logic/score previews (grammar normative in `design/15_expression_language.md`).
- **The Scorer engine (OD-16, SP1/2a/2b)** — `questionnaire-scorer/` (raw-wasm Scorer ABI v1 + the TS host `compileScorer`/`runScorer` + the reference PHQ-9 + the conformance runner). The scoring builder's live preview can run a Scorer the same way the viewer does.
- **The Library Core read API** — `library/` (live at https://questionnaire-library.vercel.app and locally): `GET /v1/questionnaires`, `/entities/{type}/{id}`, `/.../definition?resolved=true`, `/search`, `/facets`. The Library browser + pick-from-Library use this. **NOTE: Library is READ-ONLY (Git-ingest); there is no write/contribution API.**
- **The Viewer Service** — `viewer-service/` deployment API (`POST /v1/deployments`, presets/dimensions) for Preview deployments; the shared **denormaliser** (`questionnaire-runtime-denormaliser`) for turning a draft into a previewable runtime.

**Scope flags / what's BLOCKED (do not start these; design around them):**
- **Identity/auth (OD-08) is NOT built.** So **projects, collaborators/roles/invites (cap. 1), submission-to-Library / contribution PRs / review threads (cap. 11), and translation-memory across users** all depend on it — **defer them.** Start with **single-user, local/offline authoring + preview + export** that needs no auth. Library **read** (pick-from-Library) works today; Library **write** does not.
- The **canonical JSON is the source of truth** — the Editor reads/writes Schema 2 (`schemas/questionnaire/`, v26.0609) and must round-trip valid against it. Hard-pin references `@vYY.MMDD` (OD-06); never silently upgrade.

**Stack + process:** Vite + React 19 + TS + Tailwind (mirror `library-web/`). Standing pattern — **brainstorm the decomposition + each stage WITH the owner → spec (`docs/superpowers/specs/`) → plan (`docs/superpowers/plans/`) → subagent-driven TDD build → review → merge to master locally + push (NO PRs).** Owner reacts to **screenshots** — show, don't describe (Playwright chromium is installed; reuse the `library-web/` script pattern). The Editor is a visual product — the **`frontend-design` skill** helps execution quality, and the theme system already gives a polished default look.

**Authoritative refs:** **[design/07_editor.md](design/07_editor.md)** (the full Editor design + user flows); [design/05_data_model.md](design/05_data_model.md) + [design/05a_reusable_entities.md](design/05a_reusable_entities.md) (Schema 2 + the OD-15 entity model); [design/05b_scoring.md](design/05b_scoring.md) (scoring/Scorer, OD-16); [design/15_expression_language.md](design/15_expression_language.md) (the expression grammar); [design/06_library.md](design/06_library.md) (the Library it reads); ODs **OD-01** (custom React, no SurveyJS), **OD-03** (renderer-lib preview), **OD-05** (reference overrides), **OD-06** (hard-pin + CalVer), **OD-11** (WASM evaluator), **OD-12** (5-concept structure), **OD-15** (entity model), **OD-16** (scoring), **OD-18** (shared denormaliser for preview). Memories: `project_od15_reusable_entity_pivot`, `project_pagination_model`, `project_od16_scoring_resolved`, `feedback_give_direct_recommendations`, `feedback_no_pr_just_merge_push`.

---

## Current status (what's built vs not)

| Area | Status |
|---|---|
| **8 data-model schemas** | ✅ authored, tagged, validated (`schemas/`, 309 tests, 44 examples). Schemas 1 + 2 at **v26.0609**. **Kept in-repo; public hosting at `behaverse.org/schemas/` deferred** (out of MVP scope, owner decision 2026-06-10); the `$id`/`$ref` URLs stay as canonical identifiers the validator resolves locally. |
| **21 open decisions** | ✅ all resolved (`design/10_open_decisions.md`); latest **OD-21** (instrument-family grouping, 2026-06-09). |
| **Repo topology** | ✅ decided + locked (`design/14_repository_topology.md`). Multi-repo split **deferred** (see below). |
| **Library Core** (sub-project 1) | ✅ **built + merged** under `library/` — Python/FastAPI/PostgreSQL, `jsonb` source-of-truth + derived index ("Approach C"), Git-backed ingestion, public read API (`/v1` questionnaires + entities + dependents + search + facets + definition + withdrawn→410), filters + CORS. No auth, no write API (Git-ingest only); published+withdrawn lifecycle only. |
| **Legacy importer** (sub-project 2) | ✅ **built + merged** under `library/src/library/importers/survey_db/` — `survey_db.sqlite` → canonical Schema 2 JSON (+ provenance + loss report). Smoke test converts all (793 prompts, 64 questionnaires), validates every artifact, ingests into Postgres with zero errors. Emits `instrument_id` (OD-21) + computed `item_count`. |
| **Library web UI** (sub-project 5) | ✅ **built + merged** under `library-web/` — Vite + React 19 + TS + Tailwind read-only catalogue SPA (search → view metadata + items → download canonical JSON). Instrument-family grouping, collapsible facets, item counts, variant rendering. |
| **BDM upstream deviations D1–D6** | ✅ drafted + filed as issues upstream in `behaverse/data-model`; the deviation log remains in `design/05c_bdm_alignment.md`. |
| **Contribution/review workflow + DOI** (sub-project 3) | ❌ not started — needs auth/Identity (OD-08). |
| **Community signals** (sub-project 4) | ❌ not started — needs Identity. |
| **Runtime denormaliser** (Phase 2, sub-project 1) | ✅ **built + merged** to `master` (2026-06-10) under `questionnaire-runtime-denormaliser/` — pure, I/O-free Python lib: Schema 2 → Schema 3 (resolve refs → locale-trim → reconcile manifest → pin scorers → strip scoring → provenance). Injectable `resolve_entity`; collect-all `PreflightError`; `canonical_hash` for the future cache key. 56 tests. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-10-runtime-denormaliser*`. |
| **Viewer Service — VS-A (runtime generation core)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-10) under `viewer-service/` — FastAPI+Postgres service that mints cached Schema 3 runtimes (OD-18f 5-tuple `runtime_cache` + LRU + admin purge) by calling the denormaliser, reading from the Library via a new additive **resolution-bundle** endpoint (`GET /v1/questionnaires/{id}/versions/{v}/resolution-bundle`, added to `library/`). Viewer registry (Schema 7 manifests, direct POST upload), minimal deployments, deployment-level locale. HTTP-only Library access; no sessions/auth (VS-B/C). 26 tests. Decomposed into **VS-A → VS-B → VS-C**. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-10-viewer-service-vs-a*`. |
| **Viewer Service — VS-B (sessions + submission + forwarding)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-10) under `viewer-service/` — the participant data path: anonymous session mint (+ opaque hashed token + Schema 3 runtime), core resume (OD-14 sub-q3/q6) + locale switch, response/event submission (Schema 5/4a validated) to a durable Postgres `outbox`, the **OD-13** forwarder (`process_outbox_batch` + `forward-worker` CLI, backoff/max-attempts/tamper-check + `HTTPBehaverseSink`), two-tier bounds (503 hard cap). Lifecycle `in_progress→submitted→forwarded`. 60 VS tests. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-10-viewer-service-vs-b*`. |
| **Viewer Service — VS-C (deployment management & lifecycle)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-11) under `viewer-service/` — full deployment record (mode presets + 4 dimensions + `active_from`/`active_until` + `quota` + style/flow overrides + channels) via idempotent `ALTER`s; deployment CRUD (create/get/list/patch); wires the deferred **OD-14** rules into mint/resume (active-window + quota gating → 410/409; ephemeral refuse-resume → 409; ephemeral submissions validated but skip the outbox; asymmetric `active_until`). Delivers **UC-04 anonymous-link + UC-08 demo**. `mode_preset` defaults `anonymous_link` (demo supported; others rejected until Identity/Platform). 88 VS tests. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-viewer-service-vs-c*`. |
| **Viewer Service — VS-D (response export / CSV serializer)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-11) under `viewer-service/` — gate-blocking **UC-11**: `GET /v1/deployments/{id}/export.csv` streams a BDM-native CSV of all collected responses for a deployment (pure `export_csv` serializer: 72-column header derived from Schema 5 `Response`, non-scalar cells JSON-encoded; `iter_response_rows` flattens outbox `ResponseSet`/bare-`Response` payloads, events excluded, demo/ephemeral absent; own-connection `StreamingResponse`). 98 VS tests. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-viewer-service-vs-d*`. |
| **Viewer Service — VS-E (monitoring dashboard + theme infrastructure)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-11) under `viewer-service/` — `GET /v1/deployments/{id}/metrics` per-deployment snapshot (active/completion/quota/recent + OD-13 forwarding alert; SSE + abandonment-hotspots deferred); theme store + `POST/GET /v1/themes` with WCAG-AA-at-save (contrast ≥4.5:1 + base_size≥14) + built-in themes seeded by `migrate` + `/sessions/new` theme injection. 116 VS tests. **The Viewer Service is now FEATURE-COMPLETE for Phase 2 (VS-A..E).** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-viewer-service-vs-e*`. |
| **Web Viewer — WV-A (shell + bootstrap + renderer)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-12) under `web-viewer/` — Vite+React19+TS+Tailwind SPA; URL contract (`?deployment=&locale=&viewer_url=` + dev `?fixture=`); `POST /v1/sessions/new` bootstrap + theme→CSS-vars; faithful-projection Schema 3 renderer (`src/renderer/` = OD-03 library boundary: option merge on `index`, widget derivation per design/05a §13, items/messages/sections + `shared_option` matrix, UnsupportedElement honesty); **focus presentation default** (one question per view, choice cards + letter hints, auto-advance, `style.x_presentation`/`x_auto_advance` overrides — see design/08 §"Presentation modes"); step navigation + required gating + spec focus management; chrome en/pt; Schema 7 `manifest.json` (no `logic_actions` → VS strips logic) validated in `npm test`; +additive **VS CORS** (`VS_CORS_ORIGINS`). 80 vitest tests; **live smoke passed** (real imported AISS via Library→denormaliser→VS mint). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-web-viewer-wv-a*`. |
| **Web Viewer — WV-B (response capture + submission)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-12) under `web-viewer/` — **closes the Phase-2 linear gate.** Schema 5 rows per attempt per item on forward-advance (ALL attempts kept: `x_response_revises`/`x_response_revision`; messages = `acknowledged` instruction trials; ALL durations SECONDS), `bdm:` event batches (5 s/20, canonical trial grammar), serial backoff SubmissionQueue (422-drop, keepalive flush on pagehide/visibilitychange), finishing flow (flush → `/complete` → thank-you, visible retry), `style.x_summary_rt: false` opt-out. Rows/events Ajv-validated against the REAL schemas in tests. +Additive VS change: mint/GET return `agent_id`+`session_index`. **Live gate smoke PASSED** (chromium completed AISS → export.csv 21 rows, RTs in seconds, session `submitted`). 110 web-viewer + 120 VS tests. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-web-viewer-wv-b*`. |
| **Expression Evaluator — WV-C / OD-11** (Phase 2) | ✅ **built + merged** to `master` (2026-06-12) under `questionnaire-expression-evaluator/` — the canonical reference evaluator (Rust→WASM) all viewers + Editor embed for IDENTICAL evaluation of `Expression` strings. `core/` crate (lexer→recursive-descent parser→deterministic evaluator; `Value` lattice; `/0`→Null, code-point string order, never-panics sentinel-Null, `condition()`=non-Bool→false) + `web/` (`wasm-bindgen` package `@behaverse/expression-evaluator`: `evaluate_condition`/`check_expression`/`reversed`/`compare`). Functions: `length/is_empty/not_empty/count/contains/score` (both `score('id')`+bare-id forms). Helpers `reversed_value` (05b 4.1) + `compare_solution` (equals/set_equals/matches_regex, OD-16 4.3). **Determinism proven**: one `test_vectors.json` run identically by the Rust host AND compiled WASM (24 host + 31 WASM tests). Grammar is now normative in **design/15_expression_language.md**. Rust toolchain installed (rustc/cargo 1.96.0, wasm32, wasm-pack 0.13.1). Godot C-ABI + Editor wasmer-python bindings DEFERRED (no consumer yet). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-expression-evaluator-wv-c*`. |
| **Web Viewer — WV-D (logic, validation, in-session scoring)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-12) under `web-viewer/src/logic/` — embeds the WV-C evaluator (built `--target web`, lazy-loaded behind a `LogicEvaluator` port; engine tests inject a fake) to drive `show_if` visibility, **skip/branch graph-walk navigation** (forward-only jumps, hidden steps skipped, Back retraces a visited stack), `piping` (prompt-text), per-question (range/length/format) + cross-question validation (blocks Next + per-item messages), and scoring helpers (`reversed_value`→Schema 5 `score`, Solution `correct` at answer-commit). Manifest now declares `logic_actions`+`evaluator` (v26.0612) → **VS no longer strips logic**. `score(id)` resolves null (external Scorer deferred → score-gated branches safely don't fire, F1). 145 web-viewer tests; **live branching smoke PASSED** (real in-browser WASM branched on a fixture; denormaliser reconcile confirms logic carried for the v26.0612 manifest). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-web-viewer-wv-d*`. |
| **Web Viewer — WV-E (session resume + locale switch)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-12) under `web-viewer/src/resume/` — OD-14 resume: per-question state persists to IndexedDB (DB `behaverse-web-viewer`, store `resume`, keyed by deployment, **incl. the session token** so reload re-authenticates — resolves WV-A's in-memory-token caveat). Boot tries resume before minting (`resolveResume` branches on `GET /sessions/{id}`: in_progress→rehydrate answers+visited & land at the first required-unanswered visible step ELSE the saved position; submitted→"already completed"; 409 ephemeral→wipe+fresh+demo-notice; invalid→fresh; network→retry). `LocaleSwitcher` (>1 available_locales) swaps runtime text via `POST /sessions/{id}/locale` with answers intact. Debounced (500 ms) per-question persistence; demo/ephemeral + fixture never persist. +Additive VS change: `/sessions/new` returns `ephemeral`. 167 web-viewer + 122 VS tests; **live resume smoke PASSED** (real chromium reload restored answers/token/position via IndexedDB; caught + fixed an all-optional landing bug). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-web-viewer-wv-e*`. |
| **Web Viewer — WV-F (conformance + distribution)** (Phase 2) | ✅ **built + merged** to `master` (2026-06-12) under `web-viewer/` — **the FINAL Web Viewer stage; WV-A..F COMPLETE.** (A) Renderer-as-library (**OD-03**): `npm run build:lib` → `dist-lib/` (`@behaverse/questionnaire-renderer`: ESM + dts + precompiled CSS, React externalized; the Editor preview consumes it). (B) **PERF-01**: boot overlaps the 1 MB evaluator WASM load with the mint round-trip (Promise.all; PERF.md budget — shell ~81 KB gzip). (C) iframe: `embed.ts` posts `behaverse:loaded/completed/resize` to a host frame (optional `?embed_origin=`). (D) **PWA**: `vite-plugin-pwa` precaches the shell + WASM, installable webmanifest, `/v1/` NetworkOnly (cached-mint impossible). (E) Schema 7 manifest served at `/manifest.json` (build-generated from the canonical one; now correctly declares `resume`+`locale_switching` true). 173 web-viewer tests; **live PWA/iframe smoke PASSED**. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-web-viewer-wv-f*`. |
| **Visual Polish — Stage 1 (theme system)** | ✅ **built + merged** (2026-06-13) under `web-viewer/` — data-driven `ViewerTheme` token model + registry + `applyTheme` (viewer-side presets; VS bundle palette still overrides; no Schema-7 bump); synced `qv-theme` CSS in `index.css`↔`lib.css`; universal polish (focus-ring, optical centring, transitions, card states); **Minimal** (default) + **Sage** + **Artsy** themes; dev-only registry-driven **gallery** at `/gallery.html`; per-theme WCAG-AA contrast test; authoring runbook **`web-viewer/THEMES.md`**. 202→213 web-viewer tests. **Stage 2** (6 more themes + optional VS-bundle token extension) deferred. Memory `project_web_viewer_visual_polish_stage1`. |
| **OD-16 Scoring — SP1 (engine + conformance runner)** | ✅ **built + merged** (2026-06-13) under `questionnaire-scorer/` (standalone) — normative raw-wasm **Scorer ABI v1** (`ABI.md`), reference **PHQ-9** scorer (Rust→wasm), TS host `compileScorer`/`runScorer`, conformance runner + `scorer-conformance` CLI (sha256→ABI→output_schema→test_cases→determinism). 9 cargo + 6 host tests. Memory `project_scorer_conformance_runner_sp1`. |
| **OD-16 Scoring — SP2a (live branching)** | ✅ **built + merged** (2026-06-14) — **`score(id)` runs live** so score-gated branching fires at page-submit. Browser **ScorerExecutor** (`web-viewer/src/scoring/`): fetch pinned wasm + **sha256-verify** + compile + cache + JSON-Pointer resolve, replacing `nullResolver` (sync via boot-compile + page-submit refresh; sentinel-null on failure). VS **serves** the wasm (`GET /v1/scorers/{ref}/impl.wasm` + `VS_SCORER_MAP`) + **rewrites `impl.url` at mint** (`VS_PUBLIC_BASE`); PHQ-9 made lenient. Live smoke: PHQ-9 branched on the computed total. Memory `project_web_viewer_scoring_sp2a`. |
| **OD-16 Scoring — SP2b (display + persistence)** | ✅ **built + merged** (2026-06-14) — themed **ScoreSummary** panel (named scores, em-dash for null) at **terminal + live**; denormaliser emits the display policy as root **`x_show_score`/`x_show_score_live`** (via the `^x_` extension — no Schema-3 bump); **`scorer_outputs` persisted** to the session (JSONB) via `POST /v1/sessions/{id}/scorer_outputs` (Schema-6-validated, ephemeral-skip). Live smoke: PHQ-9 finish showed Total 12 / moderate / Moderate Depression. 213 web-viewer + 134 VS tests. Memory `project_web_viewer_scoring_sp2b`. **OD-16 in-session scoring is now feature-complete for the Web Viewer.** |
| **Editor / Native (Godot) viewer / Participant Platform** (Phase 2+) | **Editor = the NEXT ACTIVE TASK** (custom React/TS authoring UI — see the "▶ ACTIVE TASK — The Editor" brief above; consumes the renderer library + WASM evaluator + Scorer + Library read API + VS preview deployments). ❌ not started: the **Native (Godot) viewer** (OD-01; must honour `style.x_presentation` + the same `bdm:`/Schema-5 contract, or declare non-support); the **Participant Platform** + Editor collaboration/contribution + Library write/DOI/community-signals (all **OD-08 Identity-blocked**). **OD-16 SP3 deferred**: forward `scorer_outputs` to Behaverse; server-side `http`/`python`/`r` scorer executors; Library scorer-artifact storage. **Web-Viewer follow-ups** (web-viewer/FOLLOWUPS.md): fully-lazy evaluator load (PERF F3); theme not re-fetched on resume; promote `x_response_*` attempt fields to first-class at next Schema 5 CalVer; renderer-lib npm publish at the repo split. Deferred: Behaverse reconciliation + `validated` state (Behaverse-blocked). |
| **Deployment + persistent content seeding** | ✅ **DEPLOYED + LIVE (2026-06-10)** — **https://questionnaire-library.vercel.app** (Vercel SPA + FastAPI serverless, same-origin) reading a seeded Supabase Postgres (`questionnaire-library`, eu-central-1). 64 questionnaires / 54 instrument families live. **Phase 1 shipped.** See `project_mvp_deployed` memory (deploy gotchas) + `scripts/seed-supabase.md`. |
| **Public schema hosting at `behaverse.org/schemas/`** | ⏸️ **deferred** — schemas kept in-repo for now (owner decision 2026-06-10); out of MVP scope, revisited post-MVP. |

**Running the suites** (note the Docker quirk; **run each suite separately** — see below):
```bash
source .venv/bin/activate
DOCKER_CONFIG=/tmp/lib_docker pytest library/ -q     # 126 — integration tests use ephemeral Postgres via testcontainers
DOCKER_CONFIG=/tmp/lib_docker pytest viewer-service/ -q   # 134 — VS-A..E + CORS + session identity + scorer serving/persistence (testcontainers Postgres)
pytest questionnaire-runtime-denormaliser/ -q        # 57 — denormaliser (pure)
pytest tools/tests/ -q                               # 309 — schema validation (44 examples)
( cd library-web && npm test && npm run build )      # 59 frontend (vitest) + clean build
( cd web-viewer && npm test && npm run build && npm run build:lib )   # 213 web-viewer (vitest; incl. scoring + theme + axe) + Schema 7 manifest check + renderer-lib build
bash -c '. "$HOME/.cargo/env" && cd questionnaire-scorer && cargo test'   # 9 — Scorer ABI + PHQ-9 (Rust)
( cd questionnaire-scorer/host && npm test )         # 6 — Scorer TS host + conformance runner (vitest; pretest builds phq9.wasm)
bash -c '. "$HOME/.cargo/env" && cd questionnaire-expression-evaluator && cargo test'   # 24 — expression evaluator core (Rust)
( cd questionnaire-expression-evaluator/web && npm test )   # 31 — evaluator WASM determinism vectors
```
`DOCKER_CONFIG=/tmp/lib_docker` is **required** for the library + viewer-service integration tests: `~/.docker/config.json` references a `credsStore: desktop` helper that isn't installed, so testcontainers fails without an override pointing at an empty docker config (`/tmp/lib_docker/config.json` = `{}`). **If integration tests hang** (the container is ready but the host can't reach its published port), Docker's host→container NAT has gone stale after heavy container create/remove churn — `sudo systemctl restart docker` clears it. **Run each Python suite in its own `pytest` invocation** — `library/` and `viewer-service/` each have a `tests/test_validation.py`, and pytest's default import mode raises an "import file mismatch" collection error if you collect both packages in one command. (Each package passes on its own; this is a multi-package monorepo pytest quirk, not a code defect.)

---

## What's next

The Library Core + importer + web UI close out the *buildable* MVP work. The two live tracks:

1. **MVP deployment — ✅ DONE (2026-06-10).** Phase 1 is shipped: the Library + web UI are live at **https://questionnaire-library.vercel.app** (Vercel SPA + FastAPI serverless, same-origin) on a seeded Supabase Postgres (eu-central-1). Re-seed: `scripts/seed-supabase.md`; deploy gotchas: the `project_mvp_deployed` memory. Follow-up: function is in `iad1` (US) while the DB is EU — consider moving it to `fra1`; custom domain deferred.
2. **Phase 2 — IN PROGRESS.** First deliverable shipped: the **`questionnaire-runtime-denormaliser`** (Schema 2 → Schema 3) is built + merged to `master` (2026-06-10, 56 tests). **Next = Viewer Service / Orchestrator**, then Web Viewer + WASM evaluator. **→ See the "Phase 2 — START HERE" delegation brief below** (updated to mark the denormaliser done).

Also open (not blocking the above): **contribution/review workflow + DOI** (sub-project 3) + **community signals** (sub-project 4) — both need the **Identity sibling / auth** (OD-08, out of Library-Core scope); and **library follow-ups** in `library/FOLLOWUPS.md` (item-list resolution, strictest-license surfacing, Alembic/`serve`, + the deferred variant-curation / response-scale QA items).

Standing implementation pattern: **brainstorm → spec (`docs/superpowers/specs/`) → plan (`docs/superpowers/plans/`) → subagent-driven TDD build → review → merge → push.** (11 spec/plan pairs to date.) **Finish branches by merging to master locally + pushing — no PRs on this project** (owner preference).

---

## Phase 2 — progress log (backend complete)

**Objective.** **Phase 2 — Web Viewer + Deployments**: take a questionnaire from the Library, deploy it for anonymous online use, let participants complete it, emit responses + `bdm:` events that flow into Behaverse via the Viewer Service, and let the researcher export the data. Full deliverable list + the leave-Phase-2 gate are in [plan/01_roadmap.md](plan/01_roadmap.md) §"Phase 2". **Backend status: COMPLETE** — the denormaliser + the full Viewer Service (VS-A..E) are built. The remaining gate work is the **Web Viewer** + its **WASM evaluator** (see the "Web Viewer — START HERE" brief below).

**✅ DONE — `questionnaire-runtime-denormaliser`** (built + merged 2026-06-10). The shared dependency everything else in Phase 2 needs: a pure, I/O-free Python library that turns a Schema 2 questionnaire (refs + translations + scoring) into a **Schema 3 Runtime** (references resolved, single locale, viewer-manifest reconciled, scorer impls pinned, scoring optionally stripped, provenance attached). Per **OD-18**, consumed by both the Viewer Service (session-mint) and the Editor preview. Built under `questionnaire-runtime-denormaliser/`. Public API `denormalise(...)` + injectable `resolve_entity` + `canonical_hash` (the future Viewer Service imports this for matching cache-key hashes). Key decisions locked in its spec: **faithful projection** (keep Schema 2 vocabulary/structure — the Web Viewer does the option-merge), **no Schema 3 bump** (loose canonical schema kept; shape enforced by package tests + an internal strict schema), **strict missing-locale pre-flight error**, **collect-all `PreflightError`**. Note logged in its `FOLLOWUPS.md`: the canonical Schema 2 examples have **dangling refs** (~14 reusable entities referenced but absent from `library_examples/`), so `schemas/runtime/examples/` were NOT regenerated — separate follow-up. Design: [design/05d_runtime.md](design/05d_runtime.md); spec/plan: `docs/superpowers/{specs,plans}/2026-06-10-runtime-denormaliser*`.

**✅ DONE — Viewer Service VS-A (runtime generation core)** (built + merged 2026-06-10, `viewer-service/`). The Viewer Service was decomposed into **VS-A → VS-B → VS-C**. VS-A is the runtime-generation spine + denormaliser's first consumer: Postgres `runtime_cache` (OD-18f 5-tuple key + LRU + admin purge); viewer-registry storing Schema 7 manifests (direct POST upload); minimal deployments; deployment-level locale resolution; the mint-runtime path that calls the denormaliser; and a new additive **resolution-bundle** endpoint on `library/` (returns `{definition, entities}` so the denormaliser gets entity+scorer bodies). HTTP-only Library access; **no sessions/auth** (VS-B/C). 26 tests.

**✅ DONE — Viewer Service VS-B (sessions + submission + forwarding)** (built + merged 2026-06-10, `viewer-service/`). The participant data path: `POST /sessions/new` (anonymous mint → `{session_id, session_token, runtime}`, wrapping VS-A's `mint_runtime`); opaque session tokens (stored SHA-256-hashed; `require_session` Bearer auth); core resume (`GET /sessions/{id}` + `/runtime`) + locale switch (re-mint via the viewer stored on the session); `POST /sessions/{id}/responses`·`/events` (Schema 5 / 4a validated) → durable Postgres `outbox`; `/complete`; the **OD-13** forwarder (`forwarding.process_outbox_batch` + `viewer-service forward-worker --once/--loop` CLI; exponential backoff, max-attempts, per-submission SHA-256 tamper check; `Sink` interface + `HTTPBehaverseSink`); two-tier bounds (503 hard cap). Lifecycle `in_progress→submitted→forwarded` (`validated`/`abandoned` deferred). 60 VS tests. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-10-viewer-service-vs-{a,b}*`.

**✅ DONE — Viewer Service VS-C (deployment management & lifecycle)** (built + merged 2026-06-11, `viewer-service/`). Full deployment record + CRUD; `check_deployable` (active-window + per-deployment `quota.max_sessions` gate) wired into `new_session` (mint past `active_until` → 410; before `active_from` / over quota → 409) and resume (`session.ephemeral` set at mint; ephemeral → `409 ephemeral_no_resume`; ephemeral submissions validate + 202 but skip the outbox). `mode_preset` defaults `anonymous_link`. Delivers UC-04 + UC-08. 88 VS tests.

**✅ DONE — Viewer Service VS-D (response export / CSV serializer)** (built + merged 2026-06-11, `viewer-service/`). The remaining additive VS surfaces were split **VS-D** (CSV export — gate-blocking UC-11, done) and **VS-E** (dashboard + theming — next). VS-D: `GET /v1/deployments/{id}/export.csv` streams a BDM-native CSV (pure `export_csv.response_columns`/`to_csv`; `store/export.iter_response_rows` flattens the outbox; own-connection `StreamingResponse`). 98 VS tests.

**✅ DONE — Viewer Service VS-E (monitoring dashboard + theme infrastructure)** (built + merged 2026-06-11, `viewer-service/`). `GET /v1/deployments/{id}/metrics` (per-deployment snapshot: active/completion/quota/recent-submissions + OD-13 forwarding alert; SSE + abandonment-hotspots deferred) + theme store with `POST/GET /v1/themes` (WCAG-AA contrast/min-font check at save → 422 on fail), built-in themes seeded by `viewer-service migrate`, and `/sessions/new` returning the deployment's resolved `theme` bundle. 116 VS tests. **The Viewer Service is FEATURE-COMPLETE for Phase 2 (VS-A..E): runtime gen + sessions/submission/forwarding + deployment lifecycle + export + dashboard/theming. The whole VS side of the Phase-2 gate works.**

---

## Web Viewer — START HERE (delegation brief for a new agent)

> **PROGRESS (2026-06-12): WV-A + WV-B (linear gate CLOSED) + WV-C (OD-11 expression evaluator) are ALL BUILT + MERGED.** Next stage = **WV-D — wire the now-built `questionnaire-expression-evaluator/` (`@behaverse/expression-evaluator`, `evaluate_condition`/`reversed`/`compare`) into `web-viewer/`**: compile each runtime `LogicRule.condition`/`show_if`/validation `condition` and drive branching/visibility/skip/piping + per/cross-question validation; apply `reversed_value` at answer-commit and compute Solution `correct`; in-session scoring via the Schema-3-pinned scorer impls (the `score(id)` host binding feeds the evaluator). The evaluator's grammar is normative in **design/15_expression_language.md**; the Web Viewer's Schema 7 `manifest.json` must gain `logic_actions` + an `evaluator` block once WV-D supports logic (today it declares none, so the VS strips logic — WV-D flips that on). Start WV-D with its own brainstorm→spec→plan cycle.
>
> Original WV-A note: **WV-A built** (`web-viewer/`, 80 tests, live-smoke-verified against a real Library+VS stack). **Owner notes (2026-06-12), tracked in `web-viewer/FOLLOWUPS.md`:** (1) **date questions** are not expressible (Schema 2 `input_data_type` = choice|number|text → UnsupportedElement card; workaround text+RegEx / number-year; native support would be a breaking Schema 2 bump + new OD); (2) a dedicated **visual-design + behaviour polish pass** on the viewer is wanted later (focus-ring styling, optical centring, transitions, card states) — owner-driven, after the functional stages. The decomposition is locked in the WV-A spec §0: **WV-A shell+renderer ✅ → WV-B capture+submission (NEXT — closes the linear-questionnaire gate) → WV-C WASM evaluator (OD-11, parallel-able) → WV-D logic → WV-E resume → WV-F manifest/packaging.** Owner visual directive: Typeform-class focus mode is the default (`feedback_web_viewer_visual_direction` memory; design/08 §"Presentation modes"). Start WV-B with its own brainstorm→spec→plan cycle; `web-viewer/README.md` has the dev quickstart + a verified live-VS walkthrough, `web-viewer/FOLLOWUPS.md` the deferred items. The renderer reports answers via `onAnswer(key, value)` — WV-B turns those into Schema 5 `Response` rows (+ summary RT per OD-07) and wires `/responses`, `/events`, `/complete`.

**The whole backend for Phase 2 is done.** Schemas (8, in-repo), the Library (catalogue + API + web UI, live on Vercel+Supabase), the `questionnaire-runtime-denormaliser`, and the **full Viewer Service (VS-A..E)** are built, merged, and tested. **The one remaining gate-critical Phase-2 component is the participant-facing Web Viewer** (plus its embedded WASM evaluator). This brief orients you to build it.

**Objective.** Build the **Web Viewer**: a custom **React + TypeScript** single-page app that takes a **Schema 3 runtime** from the Viewer Service, renders the questionnaire to a participant, captures responses + `bdm:` semantic events, and submits them back to the Viewer Service — which already forwards to Behaverse and exposes export/metrics. Authoritative design: [design/08_viewer.md](design/08_viewer.md) (§"Web Viewer", §"Session lifecycle", §"Session resume semantics", §"Event emission"). Stack is **locked by OD-01: custom React + TS, NO SurveyJS.** Build it under a new top-level `web-viewer/` (mirrors `library-web/`'s Vite + React 19 + TS + Tailwind setup; migrates to `questionnaire-web-viewer` at the deferred repo split).

**This is a big component — decompose it.** Like the Viewer Service, brainstorm a decomposition first (each sub-stage its own spec→plan→build). A natural slicing:
1. **App shell + session bootstrap** — read deployment/session params from the URL, call `POST /v1/sessions/new` (→ `{session_id, session_token, runtime, theme}`), hold the token, render the runtime's metadata/first page. Apply the returned `theme` bundle.
2. **Schema 3 renderer** — render `pages[].elements[]`: the faithful-projection Option shapes (do the `options[]` + `content.<locale>.options[]` merge the denormaliser intentionally left for the viewer), the widget kinds from the Option `(input_data_type, measurement_type, selection)` triple, prompts/instructions/context. WCAG 2.1 AA (keyboard nav, screen-reader, contrast). Responsive per the §"visual fidelity" physical-envelope rule.
3. **Response capture + submission** — collect answers, build Schema 5 `Response`/`ResponseSet` rows, `POST /v1/sessions/{id}/responses` (per-item, debounced) + `/complete`; batch `bdm:` events (Schema 4a) to `/events` (every ~5 s / 20 statements); summary RT on `answered` (OD-07).
4. **Navigation + logic** — pages/sections/blocks, `show_if`, branching/skip — which needs the **WASM expression evaluator** (next bullet).
5. **Session resume** (OD-14) — persist per-question state to IndexedDB + mirror to the VS per-item endpoint; on reload `GET /v1/sessions/{id}` + `/runtime` (resumes in `last_active_locale`); locale switch via `POST /v1/sessions/{id}/locale`; ephemeral (demo) deployments get `409 ephemeral_no_resume` → mint fresh.
6. **Schema 7 conformance manifest** — publish a manifest (the viewer's supported widgets/logic-actions/scorer-impl-kinds/locales) at a stable URL; operators register it with the VS (`POST /v1/viewers`).

**Hard dependency — the WASM expression evaluator (OD-11), not yet built.** Logic (`LogicRule.condition`), validation, and scoring (`score(id)`) must evaluate via a **single Rust→WASM module** ([design/08_viewer.md](design/08_viewer.md) §"Reference evaluator"; OD-11). It's embedded by the Web Viewer (wasm-bindgen), the Native Viewer, and the Editor preview, so it's a shared deliverable — build/scope it as its own sub-project (likely before/with renderer step 4). Until it exists, steps 1–3 (linear questionnaires, no branching/scoring) are buildable; branching + in-session scoring need it.

**What the Web Viewer talks to — all built and ready (`viewer-service/`, base `/v1`):**
- `POST /sessions/new` `{deployment_id, viewer_id, viewer_version, locale?}` → `{session_id, session_token, runtime, theme}` (Bearer token for all later calls).
- `GET /sessions/{id}` (status + `last_active_locale` + outbox counts) · `GET /sessions/{id}/runtime` (resume) · `POST /sessions/{id}/locale` (switch+re-mint).
- `POST /sessions/{id}/responses` (Schema 5) · `/events` (Schema 4a) · `/complete`.
- `POST /v1/viewers` (register this viewer's Schema 7 manifest) · `POST /v1/deployments` (create a deployment to test against) · `GET /v1/deployments/{id}/export.csv` + `/metrics` (verify collected data).
- Run the VS locally: `viewer-service migrate` then `uvicorn viewer_service.api.app:create_app --factory --reload` with `DATABASE_URL` + `LIBRARY_BASE_URL` set (see `viewer-service/README.md`).

**Stack + process.** Vite + React 19 + TS + Tailwind (mirror `library-web/`). Standing pattern: **brainstorm → spec → plan → subagent-driven TDD build → review → merge+push (no PRs).** Note `library-web/`'s testing setup (vitest + RTL; a Playwright e2e smoke exists but Chrome isn't installed in this env).

**Key references:** [design/08_viewer.md](design/08_viewer.md) (Web Viewer + cross-viewer contract + OD-14 resume table), [design/08a_viewer_service.md](design/08a_viewer_service.md) (the service it calls), [design/05d_runtime.md](design/05d_runtime.md) + `schemas/runtime/` (Schema 3 it renders), `schemas/viewer_conformance/` (Schema 7 it publishes), `schemas/response/` + `schemas/events/` (what it emits), OD-01 (custom React, no SurveyJS), OD-11 (WASM evaluator), OD-07 (channels), OD-14 (resume). The `viewer-service/` README + the `project_viewer_service_vs_*` memories describe the live endpoints.

**Dependencies already in place:** all 8 schemas (✅ in-repo, v26.0609 etc.) and the Library (✅ built, serves Schema 2 via `/v1/.../definition`). The deployed MVP Library (Supabase + Vercel, in progress) gives Phase 2 a live source to read from.

**Stack (locked):** Python + FastAPI + Postgres for the Viewer Service (OD-04); custom React + TypeScript for the Web Viewer (OD-01); Rust→WASM for the evaluator (OD-11). No SurveyJS anywhere.

**Process:** the standing pattern — **brainstorm with the owner → spec → plan → subagent-driven TDD build → review → merge + push** (no PRs). Each Phase-2 deliverable is its own spec/plan cycle; start by brainstorming the denormaliser.

**Key references:** [design/05d_runtime.md](design/05d_runtime.md) (Schema 3 + denormaliser), [design/08_viewer.md](design/08_viewer.md) (Web Viewer), [design/08a_viewer_service.md](design/08a_viewer_service.md) (Viewer Service), [design/04_architecture.md](design/04_architecture.md); ODs 18 (runtime production model), 11 (WASM evaluator), 01 (custom renderer, no SurveyJS), 13 (queued forwarding), 14 (session resume), 16 (scoring); `schemas/runtime/` + `schemas/viewer_conformance/` + `schemas/events/` + `schemas/response/` + `schemas/session/`.

---

## The built components — orientation

### Library Core + importer (`library/`)
Python/FastAPI/Postgres; `jsonb` source-of-truth + derived index (Approach C); Git-backed read-only ingestion (no write API/auth). Run it locally:
```bash
source .venv/bin/activate
docker run -d --name pg -e POSTGRES_PASSWORD=pg -e POSTGRES_DB=library -p 55432:5432 postgres:16
export DATABASE_URL=postgresql://postgres:pg@localhost:55432/library
python -m library.cli migrate
python -m library.cli import-survey-db survey_database/data/survey_db.sqlite --out /tmp/content --release v26.0606 --imported-at 2026-06-06T00:00:00Z
python -m library.cli ingest /tmp/content --release v26.0606
export LIBRARY_CORS_ORIGINS=http://localhost:5173   # for the SPA
uvicorn library.api.app:create_app --factory --reload    # serves /v1 + /openapi.json
```
Public read API (base `/v1`; routes in `library/src/library/api/`; `/openapi.json` is the authoritative contract):

| Endpoint | Returns |
|---|---|
| `GET /v1/questionnaires?q=&domain=&population=&language=&license=&instrument=&min_items=&max_items=&sort=&limit=&offset=` | **`PaginatedGroups`** — instrument-**grouped** catalogue (OD-21): `{items:[InstrumentGroup], total, limit, offset}`, `InstrumentGroup = {instrument_id, title, form_count, languages, domain, forms:[CatalogueCard]}`. Singletons are one-form groups. |
| `GET /v1/questionnaires/{id}` · `/versions` · `/versions/{version}` | `EntitySummary` / `VersionInfo[]` / `EntitySummary` (metadata-only — item content comes from `/definition`). |
| `GET /v1/questionnaires/{id}/versions/{version}/definition?resolved=true` | full canonical Schema 2 JSON with refs inlined (download); **`410`** if withdrawn. |
| `GET /v1/entities/{type}` · `/{id}` · `.../dependents` | reusable entities + dependency graph. |
| `GET /v1/search?q=&type=` · `GET /v1/facets?facet_type=` | flat `PaginatedCards` / `{facet_type, values:[{value,count,label?}]}` (facets: domain/population/**instrument**/language/license; `instrument` returns human-readable name labels). |
| `GET /healthz` · `GET /openapi.json` | health / OpenAPI. |

Error envelope `{error:{code,message,detail?}}` (404 unknown, 410 withdrawn, 422 bad params).

### Library web UI (`library-web/`)
Vite + React 19 + TS + Tailwind + TanStack Query — a read-only catalogue SPA. Renders item content from `/definition?resolved=true` (the detail endpoints are metadata-only). The catalogue collapses instrument variants into expandable rows; each card shows Domain / Items / Languages / License; collapsible facet sidebar (instrument facet shows names); markdown content; per-form JSON download (blob-fetch for cross-origin). Dev: `cd library-web && npm run dev` (needs the API on :8000 + `LIBRARY_CORS_ORIGINS=http://localhost:5173`). Unit/RTL via vitest; an e2e Playwright smoke exists (needs Chrome — not installed here).

---

## Repository topology & GitHub

- **Locked plan:** multi-repo under the existing `github.com/behaverse` org, `questionnaire-` prefix — full catalogue + rationale in `design/14_repository_topology.md` (folded into `design/12_governance.md`).
- **The split is DEFERRED.** Doing it now would break the build (the library reads `schemas/` by relative path and is `pip install -e`'d in place; separating them needs cross-repo schema packaging that doesn't exist) and there's no second active component to justify the churn. Prerequisite: cross-repo schema packaging + a second component in active dev.
- **Off-machine backup exists:** `master` is pushed to a **temporary private repo `behaverse/questionnaire-tmp`** (tracked files only; to be deleted once the split is finalized — *not* the long-term home). The git account `p15e` is an **admin** of the `behaverse` org. master is local-only otherwise; push happens at branch-finish (no PRs).
- The whole ecosystem still lives in this one local repo for now.

---

## What this project is

The **Questionnaire Apps Ecosystem** is an open, modular platform for designing, distributing, and analysing **psychological-research questionnaires** and **cognitive tasks**. One canonical questionnaire definition renders consistently in a Web Viewer, a Native (Godot) Viewer, or as PDF; responses + interaction telemetry flow into a sibling project ([Behaverse](https://behaverse.org)) via a Viewer Service. Components: **Library** (catalogue + content store + web UI; built), **Editor**, **Viewer Service / Orchestrator**, **Engine family** (Web + Native viewers), **Participant Platform**. See `design/04_architecture.md`.

---

## Repository layout

| Path | What's there | Editable? |
|---|---|---|
| `design/` | **Authoritative design** (16 docs incl. `14_repository_topology.md`). What the system *is*. | Yes — follow the design/plan separation. |
| `plan/` | **Roadmap + phasing.** When/how it gets built. | Yes. |
| `schemas/` | The 8 implemented JSON Schemas (+ `versions/`, `CHANGELOG`, `examples/`). | Only at version-bump boundaries (OD-06: published versions immutable). |
| `tools/` | Schema validator + tests (`validate_schemas.py`, 309 tests). | Yes. |
| `library/` | **The built Library Core + importer** (Python/FastAPI/Postgres). `src/library/` + `tests/` + `FOLLOWUPS.md`. → `questionnaire-library-service` at the reorg. | Yes. |
| `library-web/` | **The built Library web UI** (Vite/React 19/TS/Tailwind read-only SPA). → `questionnaire-library-web` at the reorg. | Yes. |
| `docs/superpowers/` | Implementation specs + plans (11 pairs, one per build). | Yes (new pairs for new work). |
| `archive_do_not_edit/` | Superseded scattered notes. | **No** — read-only. |
| `qv_godot/`, `survey_system/` | Prototypes, reference-only; **untracked, stay local**. | Reference-only. |
| `survey_database/` | Legacy Python/SQLite catalogue — the importer's input (`data/survey_db.sqlite`). **Untracked, stays local** (not backed up to GitHub). | Reference-only. |

(`HANDOFF.md`, `my_comments.md`, and any `TOFIX.md` are working files — **not git-tracked**.)

---

## Schema inventory — what's shipped

| # | Schema | Live version | Tag |
|---|---|---|---|
| 1 | Instrument Metadata | v26.0609 (`author` singular; + optional `instrument_id`/`variant` per OD-21) | `instrument-v26.0609` |
| 2 | Questionnaire Definition | v26.0609 (retargets Schema 1 v26.0609) | `v26.0609` |
| 3 | Questionnaire Runtime | v26.0603 | `runtime-v26.0603` |
| 4a | Event Data | v26.0605 | `events-v26.0605` |
| 4b | Behavioural Channels (Mouse + Keyboard) | v26.0605 | `recordings-v26.0605` |
| 5 | Response Data | v26.0603 | `response-v26.0603` |
| 6 | Session Metadata | v26.0603 | `session-v26.0603` |
| 7 | Viewer Conformance Manifest | v26.0603 | `viewer_conformance-v26.0603` |

**Archived:** Schema 1 v26.0528, v26.0605; Schema 2 v26.0528, v26.0601, v26.0602. **Cross-version retarget DONE (2026-06-09, OD-21):** Schema 2 v26.0609 retargets Schema 1 v26.0609, bundling the `authors`→`author` rename + the new optional `instrument_id`/`variant` fields. The validator registry resolves all archived + live `$id` URLs (whether or not they're publicly hosted).

---

## Active conventions — must follow

- **CalVer `vYY.MMDD`** for everything project-owned; published versions immutable; `severity` tag per bump.
- **Hybrid entity versioning** (impl): reusable entities have no `version` field; questionnaires keep `id`/`version` in `metadata`; ingestion reads an explicit version if present else stamps `--release`.
- **`bdm:` namespace** for the Events vocabulary (OD-19); reject mixed xAPI/Schema.org/AS2 in emitted statements.
- **`additionalProperties: false` + `^x_` patternProperties** on project-owned objects.
- **Hard-pinned references** `entity_id@vYY.MMDD`; updates never silently propagate.
- **Repos:** `questionnaire-` prefix under `behaverse` org (see `design/14`). **Finish branches by merging to master locally + pushing — no PRs.**
- **Design vs. plan separation:** `design/` = what the system is; `plan/` = when/how it's built. Build *status* (and the schema-hosting decision) goes in `plan/` + this file, never in `design/`.

---

## Persistent memory

Auto-memory at `~/.claude/projects/-home-pedro-Repos-Cursor-questionnaire-apps/memory/` (index: `MEMORY.md`, loaded each session). Notable entries: feedback notes (design/plan separation, lowercase filenames, block/section naming, direct recommendations, **no-PR merge+push**); OD resolutions (OD-01/10/12/13/14/15/16/17/18/19, **OD-21**); `project_calver_versioning`; **`project_repo_topology`** (multi-repo + deferred split + temp backup), **`project_library_core`** (built Core + hybrid versioning + DOCKER_CONFIG gotcha), **`project_survey_db_importer`** (built importer + legacy model), **`project_library_web`** (built web UI + gotchas), **`project_instrument_id_grouping`** (OD-21 grouping + re-seed gotcha), **`project_schema_hosting_deferred`** (this decision).

---

## Things NOT to do

- **Don't bump a schema version casually** — CalVer bumps are breaking per OD-06 (spec/plan/execute cycle each).
- **Don't track `HANDOFF.md` / `my_comments.md` / `TOFIX.md`** — working files.
- **Don't re-decide settled ODs** — open a new OD instead of silently revising an old body.
- **Don't open PRs** — finish branches by merging to master locally + pushing (owner preference).
- **Don't execute the multi-repo split** until cross-repo schema packaging exists (it would break the build).
- **Don't commit the importer output** (`content/` is gitignored; it belongs in `questionnaire-library-content` at the reorg).
- **Don't change the schema `$id` URLs** to drop `behaverse.org` — they are canonical identifiers (resolve locally); public hosting there is deferred, not cancelled.

---

## Consistency verification (run on changes)

```bash
source .venv/bin/activate
DOCKER_CONFIG=/tmp/lib_docker pytest library/ -q   # 121 passed (Library Core + importer)
pytest tools/tests/ -q                              # 309 passed
python tools/validate_schemas.py 2>&1 | tail -3     # All 44 example(s) passed + 1 SKIP (scorer-conformance stub)
( cd library-web && npm test && npm run build )     # 59 frontend tests + clean build
grep -c "^| OD-" design/10_open_decisions.md        # 20 log rows (21 ODs; OD-02 is a merged stub, no row)
git tag -l | sort                                   # 12 schema tags (incl. instrument-v26.0609 + v26.0609)
```
