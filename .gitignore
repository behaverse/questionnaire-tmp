__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/
content/

# generated schema README artifacts (built from README.md)
schemas/*/README.html
schemas/*/README_files/
schemas/*/versions/*/README_files/
.vercel

# node deps (all packages)
node_modules/

# wasm-pack output and JS deps
questionnaire-expression-evaluator/web/pkg*/
questionnaire-expression-evaluator/web/node_modules/

# archived predecessor projects + stale snapshots (superseded; kept locally for reference)
archive/

# python build artifacts + tool scratch
*/build/
.playwright-mcp/

# Local secrets — never commit (env files, anywhere in the tree)
.env
.env.local
.env.*.local
**/.env.local
**/.env.*.local
.secrets/
.superpowers/
